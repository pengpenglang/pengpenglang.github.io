<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>HTML知识汇总</title>
    <url>/2020/07/23/HTML%E7%9F%A5%E8%AF%86%E6%B1%87%E6%80%BB/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Web学习</category>
      </categories>
      <tags>
        <tag>Web前端</tag>
        <tag>HTML</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/07/17/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p>
<h2 id="quick-start"><a class="markdownIt-Anchor" href="#quick-start"></a> Quick Start</h2>
<h3 id="create-a-new-post"><a class="markdownIt-Anchor" href="#create-a-new-post"></a> Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">"My New Post"</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p>
<h3 id="run-server"><a class="markdownIt-Anchor" href="#run-server"></a> Run server</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p>
<h3 id="generate-static-files"><a class="markdownIt-Anchor" href="#generate-static-files"></a> Generate static files</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p>
<h3 id="deploy-to-remote-sites"><a class="markdownIt-Anchor" href="#deploy-to-remote-sites"></a> Deploy to remote sites</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>CSS3知识汇总</title>
    <url>/2020/07/23/CSS3%E7%9F%A5%E8%AF%86%E6%B1%87%E6%80%BB/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Web学习</category>
      </categories>
      <tags>
        <tag>Web前端</tag>
        <tag>CSS3</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉平衡树</title>
    <url>/2020/07/23/%E4%BA%8C%E5%8F%89%E5%B9%B3%E8%A1%A1%E6%A0%91/</url>
    <content><![CDATA[<h1 id="二叉搜索树bst"><a class="markdownIt-Anchor" href="#二叉搜索树bst"></a> 二叉搜索树（<code>BST</code>)</h1>
<h2 id="概念"><a class="markdownIt-Anchor" href="#概念"></a> 概念</h2>
<p><strong>目的</strong>：为了提高查找的性能，其查找在平均情况下是O(logn)，接近二分查找效率</p>
<p><strong>定义</strong>：若其左子树不为空，则其左子树上所有结点的值都小于其根结点的值，若其右子树不为空，则其右子树上所有结点的值都大于其根结点的值</p>
<p><strong>特点</strong>：二叉搜索树的中序遍历的结果是一个<mark>非降的序列</mark></p>
<h2 id="基本操作"><a class="markdownIt-Anchor" href="#基本操作"></a> 基本操作</h2>
<h3 id="建树"><a class="markdownIt-Anchor" href="#建树"></a> 建树</h3>
<blockquote>
<p>必须有的参数时节点的值，其他的需要根据题意选择</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> val[maxn];<span class="comment">//表示节点i存的权值</span></span><br><span class="line"><span class="keyword">int</span> cnt[maxn];<span class="comment">//表示节点i存的值出现的次数，也就是每个节点包含的副本数</span></span><br><span class="line"><span class="keyword">int</span> lc[maxn];<span class="comment">//表示节点i的左孩子结点</span></span><br><span class="line"><span class="keyword">int</span> rc[maxn];<span class="comment">//表示节点i的右孩子结点</span></span><br><span class="line"><span class="keyword">int</span> siz[maxn];<span class="comment">//表示节点i的子树大小</span></span><br><span class="line"><span class="keyword">int</span> sum;<span class="comment">//寄存指针，表示节点的数量</span></span><br></pre></td></tr></table></figure>
<h3 id="中序遍历"><a class="markdownIt-Anchor" href="#中序遍历"></a> 中序遍历</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//遍历以x为根节点的二叉搜索树</span></span><br><span class="line">    <span class="keyword">if</span> (!x)<span class="keyword">return</span> ;<span class="comment">//如果为空就直接结束</span></span><br><span class="line">    print(lc[x]);<span class="comment">//先遍历左孩子</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;cnt[x];++i)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,val[x]);<span class="comment">//输出根节点的值</span></span><br><span class="line">    print(rc[x]);<span class="comment">//最后遍历右孩子</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="插入节点"><a class="markdownIt-Anchor" href="#插入节点"></a> 插入节点</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> &amp;root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//在以root为根节点的二叉搜索树中插入一个值为v的新节点</span></span><br><span class="line">    <span class="keyword">if</span>(!root)&#123;<span class="comment">//若root为空，直接返回一个值为v的新节点</span></span><br><span class="line">        root=++sum;</span><br><span class="line">        val[root]=v;</span><br><span class="line">        cnt[root]=siz[root]=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    siz[root]++;</span><br><span class="line">    <span class="keyword">if</span>(val[root]==v)&#123;<span class="comment">//若root的权值等于v，该节点的附加域该值出现的次数自增1。</span></span><br><span class="line">        cnt[root]++;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)insert(rc[root],v);<span class="comment">//若root的权值小于v，在root的右子树中插入权值为v的节点</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)insert(lc[root],v);<span class="comment">//若root的权值大于v，在root的左子树中插入权值为v的节点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="删除节点"><a class="markdownIt-Anchor" href="#删除节点"></a> 删除节点</h3>
<p>分为三种情况：要删除的节点为空，为叶节点（或者只有一个子树），有两个子树</p>
<blockquote>
<p>当节点有两个子树时最难删除，这里的做法是拿出右子树最小的节点代替被删除的节点</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-1.png" alt></p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">deletemin</span><span class="params">(<span class="keyword">int</span> &amp;root)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!lc[root])&#123;</span><br><span class="line">        <span class="keyword">int</span> u=root;</span><br><span class="line">        root =rc[root];</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">int</span> u=deletemin(lc[root]);</span><br><span class="line">        siz[root]-=cnt[root];</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> &amp;root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//删除以root为根节点的值为v的节点</span></span><br><span class="line">    siz[root]--;</span><br><span class="line">    <span class="keyword">if</span> (val[root]==v)&#123;<span class="comment">//如果找到了值为v的节点</span></span><br><span class="line">        <span class="keyword">if</span> (cnt[root]&gt;<span class="number">1</span>)&#123;<span class="comment">//如果节点的cnt大于1直接cnt--</span></span><br><span class="line">            cnt[root]--;</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(lc[root]&amp;&amp;rc[root])&#123;</span><br><span class="line">            <span class="keyword">int</span> temp=root;</span><br><span class="line">            root=deletemin(rc[root]);<span class="comment">//如果有两个非空子节点，就删除右子树中的最小值</span></span><br><span class="line">            lc[root]=lc[temp];</span><br><span class="line">            rc[root]=rc[temp];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> root=lc[root]+rc[root];<span class="comment">//如果是叶子节点就直接删除，如果只有一个儿子节点，就用这个儿子节点替换</span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)del(lc[root],v);<span class="comment">//如果当前节点的值大于v，则就去当前节点的左子树中找v</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)del(rc[root],v);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="查询值为v的节点排名"><a class="markdownIt-Anchor" href="#查询值为v的节点排名"></a> 查询值为<code>v</code>的节点排名</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">queryrnk</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的元素的排名</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]==v)</span><br><span class="line">    <span class="keyword">return</span> siz[lc[root]]+<span class="number">1</span>;<span class="comment">//左子树结点数+本身</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)</span><br><span class="line">    <span class="keyword">return</span> queryrnk(lc[root],v);</span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)</span><br><span class="line">    <span class="keyword">return</span> queryrnk(rc[root],v)+siz[lc[root]]+cnt[root];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="查询排名为k的节点值"><a class="markdownIt-Anchor" href="#查询排名为k的节点值"></a> 查询排名为<code>k</code>的节点值</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">querykth</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> k)</span></span>&#123;<span class="comment">//查找排名为k的元素的值</span></span><br><span class="line">    <span class="keyword">if</span>(siz[lc[root]]&gt;=k)<span class="comment">//当左子树的大小大于等于k时，说明排名为k的元素位于左子树中</span></span><br><span class="line">    <span class="keyword">return</span> querykth(lc[root],k);</span><br><span class="line">    <span class="keyword">if</span>(siz[lc[root]]&lt;k-cnt[root])<span class="comment">//当左子树的大小小于k - cnt时，说明排名为k的元素位于右子树中</span></span><br><span class="line">    <span class="keyword">return</span> querykth(rc[root],k-siz[lc[root]]-cnt[root]);</span><br><span class="line">    <span class="keyword">return</span> val[root];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="查询值为v的节点的前去后继节点值"><a class="markdownIt-Anchor" href="#查询值为v的节点的前去后继节点值"></a> 查询值为v的节点的前去后继节点值</h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getpre</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的节点的前驱</span></span><br><span class="line">    <span class="keyword">return</span> querykth(root,queryrnk(root,v)<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getnext</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的节点的后继</span></span><br><span class="line">    <span class="keyword">return</span> querykth(root,queryrnk(root,v)+<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="板子总结"><a class="markdownIt-Anchor" href="#板子总结"></a> 板子总结</h2>
<blockquote>
<p>这个板子没有采用数组统计存入数据再递归建树，而是直接插入</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn=<span class="number">1e5</span>+<span class="number">5</span>;<span class="comment">//存入的最大数量</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> val[maxn];<span class="comment">//表示节点i存的权值</span></span><br><span class="line"><span class="keyword">int</span> cnt[maxn];<span class="comment">//表示节点i存的值出现的次数，也就是每个节点包含的副本数</span></span><br><span class="line"><span class="keyword">int</span> lc[maxn];<span class="comment">//表示节点i的左孩子结点</span></span><br><span class="line"><span class="keyword">int</span> rc[maxn];<span class="comment">//表示节点i的右孩子结点</span></span><br><span class="line"><span class="keyword">int</span> siz[maxn];<span class="comment">//表示节点i的子树大小</span></span><br><span class="line"><span class="keyword">int</span> sum;<span class="comment">//表示节点的数量</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//遍历以x为根节点的二叉搜索树</span></span><br><span class="line">    <span class="keyword">if</span> (!x)<span class="keyword">return</span> ;<span class="comment">//如果为空就直接结束</span></span><br><span class="line">    print(lc[x]);<span class="comment">//先遍历左孩子</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;cnt[x];++i)</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,val[x]);<span class="comment">//输出根节点的值</span></span><br><span class="line">    print(rc[x]);<span class="comment">//最后遍历右孩子</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//从根节点root开始查找值为x的元素，找到就返回其下标，否则返回-1</span></span><br><span class="line">    <span class="keyword">if</span>(!root)</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">if</span>(val[root]==x)</span><br><span class="line">    <span class="keyword">return</span> root;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(val[root]&lt;x)</span><br><span class="line">    <span class="keyword">return</span> find(rc[root],x);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> find(lc[root], x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> &amp;root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//在以root为根节点的二叉搜索树中插入一个值为v的新节点</span></span><br><span class="line">    <span class="keyword">if</span>(!root)&#123;<span class="comment">//若root为空，直接返回一个值为v的新节点</span></span><br><span class="line">        root=++sum;</span><br><span class="line">        val[root]=v;</span><br><span class="line">        cnt[root]=siz[root]=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    siz[root]++;</span><br><span class="line">    <span class="keyword">if</span>(val[root]==v)&#123;<span class="comment">//若root的权值等于v，该节点的附加域该值出现的次数自增1</span></span><br><span class="line">        cnt[root]++;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)insert(rc[root],v);<span class="comment">//若root的权值小于v，在root的右子树中插入权值为v的节点</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)insert(lc[root],v);<span class="comment">//若root的权值大于v，在root的左子树中插入权值为v的节点</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">deletemin</span><span class="params">(<span class="keyword">int</span> &amp;root)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!lc[root])&#123;</span><br><span class="line">        <span class="keyword">int</span> u=root;</span><br><span class="line">        root =rc[root];</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">int</span> u=deletemin(lc[root]);</span><br><span class="line">        siz[root]-=cnt[root];</span><br><span class="line">        <span class="keyword">return</span> u;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> &amp;root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//删除以root为根节点的值为v的节点</span></span><br><span class="line">    siz[root]--;</span><br><span class="line">    <span class="keyword">if</span> (val[root]==v)&#123;<span class="comment">//如果找到了值为v的节点</span></span><br><span class="line">        <span class="keyword">if</span> (cnt[root]&gt;<span class="number">1</span>)&#123;<span class="comment">//如果节点的cnt大于1直接cnt--</span></span><br><span class="line">            cnt[root]--;</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(lc[root]&amp;&amp;rc[root])&#123;</span><br><span class="line">            <span class="keyword">int</span> temp=root;</span><br><span class="line">            root=deletemin(rc[root]);<span class="comment">//如果有两个非空子节点，就删除右子树中的最小值</span></span><br><span class="line">            lc[root]=lc[temp];</span><br><span class="line">            rc[root]=rc[temp];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> root=lc[root]+rc[root];<span class="comment">//如果是叶子节点就直接删除，如果只有一个儿子节点，就用这个儿子节点替换</span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)del(lc[root],v);<span class="comment">//如果当前节点的值大于v，则就去当前节点的左子树中找v</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)del(rc[root],v);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">queryrnk</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的元素的排名</span></span><br><span class="line">    <span class="keyword">if</span>(val[root]==v)</span><br><span class="line">    <span class="keyword">return</span> siz[lc[root]]+<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(val[root]&gt;v)</span><br><span class="line">    <span class="keyword">return</span> queryrnk(lc[root],v);</span><br><span class="line">    <span class="keyword">if</span>(val[root]&lt;v)</span><br><span class="line">    <span class="keyword">return</span> queryrnk(rc[root],v)+siz[lc[root]]+cnt[root];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">querykth</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> k)</span></span>&#123;<span class="comment">//查找排名为k的元素的值</span></span><br><span class="line">    <span class="keyword">if</span>(siz[lc[root]]&gt;=k)<span class="comment">//当左子树的大小大于等于k时，说明排名为k的元素位于左子树中</span></span><br><span class="line">    <span class="keyword">return</span> querykth(lc[root],k);</span><br><span class="line">    <span class="keyword">if</span>(siz[lc[root]]&lt;k-cnt[root])<span class="comment">//当左子树的大小小于k - cnt时，说明排名为k的元素位于右子树中</span></span><br><span class="line">    <span class="keyword">return</span> querykth(rc[root],k-siz[lc[root]]-cnt[root]);</span><br><span class="line">    <span class="keyword">return</span> val[root];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getpre</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的节点的前驱</span></span><br><span class="line">    <span class="keyword">return</span> querykth(root,queryrnk(root,v)<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getnext</span><span class="params">(<span class="keyword">int</span> root,<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//求值为v的节点的后继</span></span><br><span class="line">    <span class="keyword">return</span> querykth(root,queryrnk(root,v)+<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n;<span class="built_in">cin</span>&gt;&gt;n;<span class="comment">//存入的数量</span></span><br><span class="line">    <span class="keyword">int</span> root=<span class="number">0</span>;<span class="comment">//根节点</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;++i)&#123;</span><br><span class="line">        <span class="keyword">int</span> x;<span class="built_in">cin</span>&gt;&gt;x;</span><br><span class="line">        insert(root,x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="平衡树"><a class="markdownIt-Anchor" href="#平衡树"></a> 平衡树</h1>
<p><strong>引入</strong>：<code>BST</code>存入递增或者递减序列时会退化为一条链表，时间复杂度最坏变为O(n)</p>
<p><strong>目的</strong>：尽可能防止二叉树退化，维持树的深度为<code>logn</code></p>
<p><strong>解决</strong>：一般会给二叉搜索树附加一个平衡条件来可能<mark>限制任何节点深度过深</mark></p>
<table>
<thead>
<tr>
<th>分类</th>
<th>Treap树</th>
<th>Splay树</th>
<th>AVL</th>
</tr>
</thead>
<tbody>
<tr>
<td>限制条件</td>
<td>在二叉搜索树的基础上加了一个随机属性来保证堆的性质</td>
<td>增加一个Splay操作保持树的平衡</td>
<td>增加一个数组保证二叉搜索树的左子树和右子树的高度差不能超过1</td>
</tr>
</tbody>
</table>
<h2 id="treap树"><a class="markdownIt-Anchor" href="#treap树"></a> <code>Treap</code>树</h2>
<h3 id="概念-2"><a class="markdownIt-Anchor" href="#概念-2"></a> 概念</h3>
<p><strong>定义</strong>：treap 是一种弱平衡的二叉搜索树。<code>treap</code>这个单词是<code>tree</code>和<code>heap</code>的组合，表明<code>treap</code>是一种由树和堆组合形成的数据结构。<code>treap</code>的每个结点上要额外储存一个值<code>priority</code>（优先级）。<code>treap</code>除了要满足二叉搜索树的性质之外，还需满足父结点的<code>priority</code>大于等于两个儿子的<code>priority</code>。而<code>priority</code>是每个结点建立时随机生成的，因此<code>treap</code>是期望平衡的。</p>
<p><strong>特点</strong>：满足二叉搜索树性质并且父节点的优先级不低于子节点而定优先级（即随机属性满足堆的性质）</p>
<h3 id="基本操作-2"><a class="markdownIt-Anchor" href="#基本操作-2"></a> 基本操作</h3>
<h4 id="建树-2"><a class="markdownIt-Anchor" href="#建树-2"></a> 建树</h4>
<p>节点基本属性：<code>val</code>（节点值）和<code>dat</code>（随机值）</p>
<blockquote>
<p>默认随机函数生成的数值越大优先级越高</p>
</blockquote>
<p><strong>注意：<code>build</code>操作无论如何都要先执行，它涉及到根节点的初始化</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> ch[maxn][<span class="number">2</span>];<span class="comment">//[i][0]代表i左儿子，[i][1]代表i右儿子</span></span><br><span class="line"><span class="keyword">int</span> val[maxn];<span class="comment">//表示节点i存的权值</span></span><br><span class="line"><span class="keyword">int</span> dat[maxn];<span class="comment">//表示节点i随机出来的优先级</span></span><br><span class="line"><span class="keyword">int</span> siz[maxn];<span class="comment">//表示节点i的子树大小</span></span><br><span class="line"><span class="keyword">int</span> cnt[maxn];<span class="comment">//表示节点i存的值出现的次数，也就是每个节点包含的副本数</span></span><br><span class="line"><span class="keyword">int</span> tot,root;<span class="comment">//寄存指针和根节点</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">node</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123; <span class="comment">//新增节点，</span></span><br><span class="line">    val[++tot]=v;<span class="comment">//节点赋值</span></span><br><span class="line">    dat[tot]=rand();<span class="comment">//随机优先级</span></span><br><span class="line">    siz[tot]=<span class="number">1</span>;<span class="comment">//目前是新建叶子节点，所以子树大小为1</span></span><br><span class="line">    cnt[tot]=<span class="number">1</span>;<span class="comment">//新建节点同理副本数为1</span></span><br><span class="line">    <span class="keyword">return</span> tot;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pushup</span><span class="params">(<span class="keyword">int</span> id)</span></span>&#123;<span class="comment">//和线段树的pushup更新一样</span></span><br><span class="line">    siz[id]=siz[ch[id][<span class="number">0</span>]]+siz[ch[id][<span class="number">1</span>]]+cnt[id]; <span class="comment">//本节点子树大小=左儿子子树大小+右儿子子树大小+本节点副本数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">build</span><span class="params">()</span></span>&#123;</span><br><span class="line">    root=node(-inf),ch[root][<span class="number">1</span>]=node(inf);<span class="comment">//先加入正无穷和负无穷,便于之后操作</span></span><br><span class="line">    pushup(root);<span class="comment">//因为inf&gt;-inf,所以是右子树，</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="插入节点-2"><a class="markdownIt-Anchor" href="#插入节点-2"></a> 插入节点</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> &amp;id,<span class="keyword">int</span> v)</span></span>&#123; <span class="comment">// id依然是引用，在新建节点时可以体现</span></span><br><span class="line">    <span class="keyword">if</span>(!id)&#123;</span><br><span class="line">        id=node(v); <span class="comment">//若节点为空，则新建一个节点</span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(v==val[id])</span><br><span class="line">    cnt[id]++;<span class="comment">//若节点已存在，则副本数++;</span></span><br><span class="line">    <span class="keyword">else</span>&#123;<span class="comment">//要满足BST性质，小于插到左边，大于插到右边</span></span><br><span class="line">        <span class="keyword">int</span> d=v&lt;val[id]?<span class="number">0</span>:<span class="number">1</span>;<span class="comment">//这个d是方向的意思，按照BST的性质，小于本节点则向左，大于向右</span></span><br><span class="line">        insert(ch[id][d],v);<span class="comment">//递归实现</span></span><br><span class="line">        <span class="keyword">if</span>(dat[id]&lt;dat[ch[id][d]])rotate(id,d^<span class="number">1</span>);<span class="comment">//(参考一下图)与左子节点交换右旋，与右子节点交换左旋</span></span><br><span class="line">    &#125;</span><br><span class="line">    pushup(id); <span class="comment">//现在更新一下本节点的信息</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="旋转节点"><a class="markdownIt-Anchor" href="#旋转节点"></a> 旋转节点</h4>
<blockquote>
<p>旋转实质是在满足BST的性质的基础上比较优先级，通过交换本节点和其某个叶子节点把链叉开成二叉形状从而控制深度</p>
</blockquote>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-2.png" alt></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-3.png" alt></p>
<p><strong>注意：旋转的时候操作对象是当前节点及其子节点，与父节点无关</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span> &amp;id, <span class="keyword">int</span> d)</span></span>&#123;<span class="comment">// id是引用传递，d(irection)为旋转方向，0为左旋，1为右旋</span></span><br><span class="line">    <span class="keyword">int</span> temp=ch[id][d^<span class="number">1</span>];<span class="comment">//旋转理解：找个动图看一看就好(或参见其他OIer的blog)</span></span><br><span class="line">    ch[id][d^<span class="number">1</span>]=ch[temp][d];<span class="comment">//这里讲一个记忆技巧，这些数据都是被记录后马上修改</span></span><br><span class="line">    ch[temp][d] = id;<span class="comment">//所以像“Z”一样</span></span><br><span class="line">    id=temp; <span class="comment">//比如这个id，在上一行才被记录过，ch[temp][d]、ch[id][d ^ 1]也是一样的</span></span><br><span class="line">    pushup(ch[id][d]), pushup(id);<span class="comment">//旋转以后siz会改变，看图就会发现只更新自己和转上来的点，pushup一下,注意先子节点再父节点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="删除结点"><a class="markdownIt-Anchor" href="#删除结点"></a> 删除结点</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> &amp;id,<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> ;<span class="comment">//到这了发现查不到这个节点，该点不存在，直接返回</span></span><br><span class="line">    <span class="keyword">if</span>(v==val[id])&#123; <span class="comment">//检索到了这个值</span></span><br><span class="line">        <span class="keyword">if</span>(cnt[id]&gt;<span class="number">1</span>)&#123;</span><br><span class="line">            cnt[id]--,pushup(id);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;<span class="comment">//若副本不止一个，减去一个就好</span></span><br><span class="line">        <span class="keyword">if</span>(ch[id][<span class="number">0</span>]| ch[id][<span class="number">1</span>])&#123;<span class="comment">//发现只有一个值，且有儿子节点,我们只能把值旋转到底部删除</span></span><br><span class="line">            <span class="keyword">if</span> (!ch[id][<span class="number">1</span>]||dat[ch[id][<span class="number">0</span>]]&gt;dat[ch[id][<span class="number">1</span>]])&#123; <span class="comment">//当前点被移走之后，会有一个新的点补上来(左儿子或右儿子)，按照优先级，优先级大的补上来</span></span><br><span class="line">                rotate(id,<span class="number">1</span>),del(ch[id][<span class="number">1</span>],v); <span class="comment">//我们会发现，右旋是与左儿子交换，当前点变成右节点；左旋则是与右儿子交换，当前点变为左节点</span></span><br><span class="line">            &#125; </span><br><span class="line">            <span class="keyword">else</span> rotate(id,<span class="number">0</span>),del(ch[id][<span class="number">0</span>],v);</span><br><span class="line">            pushup(id);</span><br><span class="line">        &#125; </span><br><span class="line">        <span class="keyword">else</span> id = <span class="number">0</span>;<span class="comment">//发现本节点是叶子节点，直接删除</span></span><br><span class="line">        <span class="keyword">return</span>;<span class="comment">//这个return对应的是检索到值de所有情况</span></span><br><span class="line">    &#125;</span><br><span class="line">    v&lt;val[id]?del(ch[id][<span class="number">0</span>],v):del(ch[id][<span class="number">1</span>],v);<span class="comment">//继续BST性质</span></span><br><span class="line">    pushup(id);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询值为v的节点排名-2"><a class="markdownIt-Anchor" href="#查询值为v的节点排名-2"></a> 查询值为<code>v</code>的节点排名</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_rank</span><span class="params">(<span class="keyword">int</span> id,<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> <span class="number">0</span>; <span class="comment">//若查询值不存在，返回</span></span><br><span class="line">    <span class="keyword">if</span>(v==val[id])</span><br><span class="line">    <span class="keyword">return</span> siz[ch[id][<span class="number">0</span>]]+<span class="number">1</span>;<span class="comment">//查询到该值，由BST性质可知：该点左边值都比该点的值(查询值)小，故rank为左儿子大小 + 1</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (v&lt;val[id])</span><br><span class="line">    <span class="keyword">return</span> get_rank(ch[id][<span class="number">0</span>],v);<span class="comment">//发现需查询的点在该点左边，往左边递归查询</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> siz[ch[id][<span class="number">0</span>]] + cnt[id] + get_rank(ch[id][<span class="number">1</span>], v);<span class="comment">//若查询值大于该点值。说明询问点在当前点的右侧，且此点的值都小于查询值，所以要加上cnt[id]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询排名为k的节点值-2"><a class="markdownIt-Anchor" href="#查询排名为k的节点值-2"></a> 查询排名为<code>k</code>的节点值</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_val</span><span class="params">(<span class="keyword">int</span> id,<span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> inf;<span class="comment">//一直向右找找不到，说明是正无穷</span></span><br><span class="line">    <span class="keyword">if</span>(k&lt;=siz[ch[id][<span class="number">0</span>]])</span><br><span class="line">    <span class="keyword">return</span> get_val(ch[id][<span class="number">0</span>], k);<span class="comment">//左边排名已经大于k了，说明k对应的值在左儿子那里</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(k&lt;=siz[ch[id][<span class="number">0</span>]]+cnt[id])</span><br><span class="line">    <span class="keyword">return</span> val[id];<span class="comment">//上一步排除了在左区间的情况，若是k在左与中(目前节点)中，则直接返回目前节点(中区间)的值</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> get_val(ch[id][<span class="number">1</span>],k-siz[ch[id][<span class="number">0</span>]]-cnt[id]);<span class="comment">//剩下只能在右区间找了，k减去左区间大小和中区间，继续递归</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询值为v的节点的前去后继节点值-2"><a class="markdownIt-Anchor" href="#查询值为v的节点的前去后继节点值-2"></a> 查询值为<code>v</code>的节点的前去后继节点值</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_pre</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> id=root, pre;<span class="comment">//递归不好返回，以循环求解</span></span><br><span class="line">    <span class="keyword">while</span>(id)&#123;<span class="comment">//查到节点不存在为止</span></span><br><span class="line">        <span class="keyword">if</span>(val[id]&lt;v)</span><br><span class="line">        pre=val[id],id=ch[id][<span class="number">1</span>];<span class="comment">//满足当前节点比目标小，往当前节点的右侧寻找最优值</span></span><br><span class="line">        <span class="keyword">else</span> id=ch[id][<span class="number">0</span>];<span class="comment">//无论是比目标节点大还是等于目标节点，都不满足前驱条件，应往更小处靠近</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> pre;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_next</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> id=root,next;</span><br><span class="line">    <span class="keyword">while</span>(id)&#123;</span><br><span class="line">        <span class="keyword">if</span>(val[id]&gt;v)</span><br><span class="line">        next=val[id],id=ch[id][<span class="number">0</span>];<span class="comment">//同理，满足条件向左寻找更小解(也就是最优解)</span></span><br><span class="line">        <span class="keyword">else</span> id = ch[id][<span class="number">1</span>];<span class="comment">//与上方同理</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> next;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="板子总结-2"><a class="markdownIt-Anchor" href="#板子总结-2"></a> 板子总结</h3>
<blockquote>
<p>这个板子没有采用用数组统计存入数据再递归建树，而是直接插入，但是还是要先调用<code>build()</code>初始化根节点</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span> ll;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> maxn=<span class="number">1000019</span>,inf=<span class="number">0x3f3f3f3f</span>;<span class="comment">//存入节点最大数量</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">()</span></span>&#123;<span class="comment">//快读</span></span><br><span class="line">    <span class="keyword">int</span> out =<span class="number">0</span>,flag=<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">char</span> c=getchar();</span><br><span class="line">    <span class="keyword">while</span>(c&lt;<span class="string">'0'</span>||c&gt;<span class="string">'9'</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(c==<span class="string">'-'</span>) flag=<span class="number">-1</span>;</span><br><span class="line">        c=getchar();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span>(c&gt;=<span class="string">'0'</span>&amp;&amp;c&lt;=<span class="string">'9'</span>)&#123;</span><br><span class="line">        out=out*<span class="number">10</span>+c-<span class="string">'0'</span>;</span><br><span class="line">        c=getchar();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> flag*out;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> ch[maxn][<span class="number">2</span>];<span class="comment">//[i][0]代表i左儿子，[i][1]代表i右儿子</span></span><br><span class="line"><span class="keyword">int</span> val[maxn];<span class="comment">//表示节点i存的权值</span></span><br><span class="line"><span class="keyword">int</span> dat[maxn];<span class="comment">//表示节点i随机出来的优先级</span></span><br><span class="line"><span class="keyword">int</span> siz[maxn];<span class="comment">//表示节点i的子树大小</span></span><br><span class="line"><span class="keyword">int</span> cnt[maxn];<span class="comment">//表示节点i存的值出现的次数，也就是每个节点包含的副本数</span></span><br><span class="line"><span class="keyword">int</span> tot,root;<span class="comment">//寄存指针和根节点</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">node</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123; <span class="comment">//新增节点，</span></span><br><span class="line">    val[++tot]=v;<span class="comment">//节点赋值</span></span><br><span class="line">    dat[tot]=rand();<span class="comment">//随机优先级</span></span><br><span class="line">    siz[tot]=<span class="number">1</span>;<span class="comment">//目前是新建叶子节点，所以子树大小为1</span></span><br><span class="line">    cnt[tot]=<span class="number">1</span>;<span class="comment">//新建节点同理副本数为1</span></span><br><span class="line">    <span class="keyword">return</span> tot;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pushup</span><span class="params">(<span class="keyword">int</span> id)</span></span>&#123;<span class="comment">//和线段树的pushup更新一样</span></span><br><span class="line">    siz[id]=siz[ch[id][<span class="number">0</span>]]+siz[ch[id][<span class="number">1</span>]]+cnt[id]; <span class="comment">//本节点子树大小=左儿子子树大小+右儿子子树大小+本节点副本数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">build</span><span class="params">()</span></span>&#123;</span><br><span class="line">    root=node(-inf),ch[root][<span class="number">1</span>]=node(inf);<span class="comment">//先加入正无穷和负无穷,便于之后操作(貌似不加也行)</span></span><br><span class="line">    pushup(root);<span class="comment">//因为inf&gt;-inf,所以是右子树，</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span> &amp;id, <span class="keyword">int</span> d)</span></span>&#123;<span class="comment">// id是引用传递，d(irection)为旋转方向，0为左旋，1为右旋</span></span><br><span class="line">    <span class="keyword">int</span> temp=ch[id][d^<span class="number">1</span>];<span class="comment">//旋转理解：找个动图看一看就好(或参见其他OIer的blog)</span></span><br><span class="line">    ch[id][d^<span class="number">1</span>]=ch[temp][d];<span class="comment">//这里讲一个记忆技巧，这些数据都是被记录后马上修改</span></span><br><span class="line">    ch[temp][d] = id;<span class="comment">//所以像“Z”一样</span></span><br><span class="line">    id=temp; <span class="comment">//比如这个id，在上一行才被记录过，ch[temp][d]、ch[id][d ^ 1]也是一样的</span></span><br><span class="line">    pushup(ch[id][d]), pushup(id);<span class="comment">//旋转以后siz会改变，看图就会发现只更新自己和转上来的点，pushup一下,注意先子节点再父节点</span></span><br><span class="line">&#125; <span class="comment">//旋转实质是(&#123;在满足BST的性质的基础上比较优先级&#125;通过交换本节点和其某个叶子节点)把链叉开成二叉形状(从而控制深度),可以看图理解一下</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> &amp;id,<span class="keyword">int</span> v)</span></span>&#123; <span class="comment">// id依然是引用，在新建节点时可以体现</span></span><br><span class="line">    <span class="keyword">if</span>(!id)&#123;</span><br><span class="line">        id=node(v); <span class="comment">//若节点为空，则新建一个节点</span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(v==val[id])</span><br><span class="line">    cnt[id]++;<span class="comment">//若节点已存在，则副本数++;</span></span><br><span class="line">    <span class="keyword">else</span>&#123;<span class="comment">//要满足BST性质，小于插到左边，大于插到右边</span></span><br><span class="line">        <span class="keyword">int</span> d=v&lt;val[id]?<span class="number">0</span>:<span class="number">1</span>;<span class="comment">//这个d是方向的意思，按照BST的性质，小于本节点则向左，大于向右</span></span><br><span class="line">        insert(ch[id][d],v);<span class="comment">//递归实现</span></span><br><span class="line">        <span class="keyword">if</span>(dat[id]&lt;dat[ch[id][d]])rotate(id,d^<span class="number">1</span>);<span class="comment">//(参考一下图)与左子节点交换右旋，与右子节点交换左旋</span></span><br><span class="line">    &#125;</span><br><span class="line">    pushup(id); <span class="comment">//现在更新一下本节点的信息</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> &amp;id,<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> ;<span class="comment">//到这了发现查不到这个节点，该点不存在，直接返回</span></span><br><span class="line">    <span class="keyword">if</span>(v==val[id])&#123; <span class="comment">//检索到了这个值</span></span><br><span class="line">        <span class="keyword">if</span>(cnt[id]&gt;<span class="number">1</span>)&#123;</span><br><span class="line">            cnt[id]--,pushup(id);</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;<span class="comment">//若副本不止一个，减去一个就好</span></span><br><span class="line">        <span class="keyword">if</span>(ch[id][<span class="number">0</span>]| ch[id][<span class="number">1</span>])&#123;<span class="comment">//发现只有一个值，且有儿子节点,我们只能把值旋转到底部删除</span></span><br><span class="line">            <span class="keyword">if</span> (!ch[id][<span class="number">1</span>]||dat[ch[id][<span class="number">0</span>]]&gt;dat[ch[id][<span class="number">1</span>]])&#123; <span class="comment">//当前点被移走之后，会有一个新的点补上来(左儿子或右儿子)，按照优先级，优先级大的补上来</span></span><br><span class="line">                rotate(id,<span class="number">1</span>),del(ch[id][<span class="number">1</span>],v); <span class="comment">//我们会发现，右旋是与左儿子交换，当前点变成右节点；左旋则是与右儿子交换，当前点变为左节点</span></span><br><span class="line">            &#125; </span><br><span class="line">            <span class="keyword">else</span> rotate(id,<span class="number">0</span>),del(ch[id][<span class="number">0</span>],v);</span><br><span class="line">            pushup(id);</span><br><span class="line">        &#125; </span><br><span class="line">        <span class="keyword">else</span> id = <span class="number">0</span>;<span class="comment">//发现本节点是叶子节点，直接删除</span></span><br><span class="line">        <span class="keyword">return</span>;<span class="comment">//这个return对应的是检索到值de所有情况</span></span><br><span class="line">    &#125;</span><br><span class="line">    v&lt;val[id]?del(ch[id][<span class="number">0</span>],v):del(ch[id][<span class="number">1</span>],v);<span class="comment">//继续BST性质</span></span><br><span class="line">    pushup(id);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_rank</span><span class="params">(<span class="keyword">int</span> id,<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> <span class="number">0</span>; <span class="comment">//若查询值不存在，返回</span></span><br><span class="line">    <span class="keyword">if</span>(v==val[id])</span><br><span class="line">    <span class="keyword">return</span> siz[ch[id][<span class="number">0</span>]]+<span class="number">1</span>;<span class="comment">//查询到该值，由BST性质可知：该点左边值都比该点的值(查询值)小，故rank为左儿子大小 + 1</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (v&lt;val[id])</span><br><span class="line">    <span class="keyword">return</span> get_rank(ch[id][<span class="number">0</span>],v);<span class="comment">//发现需查询的点在该点左边，往左边递归查询</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> siz[ch[id][<span class="number">0</span>]] + cnt[id] + get_rank(ch[id][<span class="number">1</span>], v);<span class="comment">//若查询值大于该点值。说明询问点在当前点的右侧，且此点的值都小于查询值，所以要加上cnt[id]</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_val</span><span class="params">(<span class="keyword">int</span> id,<span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(!id)<span class="keyword">return</span> inf;<span class="comment">//一直向右找找不到，说明是正无穷</span></span><br><span class="line">    <span class="keyword">if</span>(k&lt;=siz[ch[id][<span class="number">0</span>]])</span><br><span class="line">    <span class="keyword">return</span> get_val(ch[id][<span class="number">0</span>], k);<span class="comment">//左边排名已经大于k了，说明k对应的值在左儿子那里</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(k&lt;=siz[ch[id][<span class="number">0</span>]]+cnt[id])</span><br><span class="line">    <span class="keyword">return</span> val[id];<span class="comment">//上一步排除了在左区间的情况，若是k在左与中(目前节点)中，则直接返回目前节点(中区间)的值</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> get_val(ch[id][<span class="number">1</span>],k-siz[ch[id][<span class="number">0</span>]]-cnt[id]);<span class="comment">//剩下只能在右区间找了，k减去左区间大小和中区间，继续递归</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_pre</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> id=root, pre;<span class="comment">//递归不好返回，以循环求解</span></span><br><span class="line">    <span class="keyword">while</span>(id)&#123;<span class="comment">//查到节点不存在为止</span></span><br><span class="line">        <span class="keyword">if</span>(val[id]&lt;v)</span><br><span class="line">        pre=val[id],id=ch[id][<span class="number">1</span>];<span class="comment">//满足当前节点比目标小，往当前节点的右侧寻找最优值</span></span><br><span class="line">        <span class="keyword">else</span> id=ch[id][<span class="number">0</span>];<span class="comment">//无论是比目标节点大还是等于目标节点，都不满足前驱条件，应往更小处靠近</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> pre;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_next</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> id=root,next;</span><br><span class="line">    <span class="keyword">while</span>(id)&#123;</span><br><span class="line">        <span class="keyword">if</span>(val[id]&gt;v)</span><br><span class="line">        next=val[id],id=ch[id][<span class="number">0</span>];<span class="comment">//同理，满足条件向左寻找更小解(也就是最优解)</span></span><br><span class="line">        <span class="keyword">else</span> id = ch[id][<span class="number">1</span>];<span class="comment">//与上方同理</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> next;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    build();<span class="comment">//不要忘记初始化[运行build()会连同root一并初始化，所以很重要]</span></span><br><span class="line">    <span class="keyword">int</span> t;t=read();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=t;i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> cmd=read(),x=read();<span class="comment">//操作选择和</span></span><br><span class="line">        <span class="keyword">if</span>(cmd==<span class="number">1</span>)</span><br><span class="line">        insert(root,x);<span class="comment">//函数都写好了，注意：需要递归的函数都从根开始，不需要递归的函数直接查询</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(cmd==<span class="number">2</span>)</span><br><span class="line">        del(root,x);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(cmd==<span class="number">3</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,get_rank(root,x)<span class="number">-1</span>);<span class="comment">//注意：因为初始化时插入了inf和-inf,所以查询排名时要减1(-inf不是第一小，是“第零小”)</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (cmd==<span class="number">4</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%d\n"</span>, get_val(root,x+<span class="number">1</span>));<span class="comment">//同理，用排名查询值得时候要查x + 1名，因为第一名(其实不是)是-inf</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (cmd==<span class="number">5</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,get_pre(x));</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (cmd==<span class="number">6</span>)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,get_next(x));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>[ ] 还差个非旋转Treap树</li>
</ul>
<h2 id="splay树伸展树"><a class="markdownIt-Anchor" href="#splay树伸展树"></a> <code>Splay</code>树（伸展树）</h2>
<h3 id="概念-3"><a class="markdownIt-Anchor" href="#概念-3"></a> 概念</h3>
<p><strong>定义</strong>：<code>Splay</code>是一种二叉搜索树，它通过不断<mark>将某个结点旋转到根结点</mark>，使得整棵树仍然满足二叉搜索树的性质，并且保持平衡而不至于退化为链表</p>
<p><strong>特点</strong>：每次操作都<code>Splay</code>一下使当前操作的节点上旋到根节点</p>
<blockquote>
<p><code>Splay</code>的主要思想就是对于查找频率较高的结点，使其处于离根结点相对较近的结点。这样就提高了查找效率。</p>
</blockquote>
<h3 id="基本操作-3"><a class="markdownIt-Anchor" href="#基本操作-3"></a> 基本操作</h3>
<h4 id="建树-3"><a class="markdownIt-Anchor" href="#建树-3"></a> 建树</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> sz;<span class="comment">//表示当前节点个数</span></span><br><span class="line"><span class="keyword">int</span> rt;<span class="comment">//表示当前根节点的编号</span></span><br><span class="line"><span class="keyword">int</span> f[N];<span class="comment">//表示编号x节点的父亲的编号</span></span><br><span class="line"><span class="keyword">int</span> key[N];<span class="comment">//表示编号为x的节点的对应的数值</span></span><br><span class="line"><span class="keyword">int</span> siz[N];<span class="comment">//表示以编号为x的节点为根的子树的节点个数</span></span><br><span class="line"><span class="keyword">int</span> recy[N];<span class="comment">//表示以编号为x的节点的数值重复次数</span></span><br><span class="line"><span class="keyword">int</span> son[N][<span class="number">2</span>];</span><br><span class="line"><span class="comment">//son[x][0]表示以编号为x的节点的左儿子的编号</span></span><br><span class="line"><span class="comment">//son[x][1]表示以编号为x的节点的右儿子的编号</span></span><br></pre></td></tr></table></figure>
<h4 id="插入节点-3"><a class="markdownIt-Anchor" href="#插入节点-3"></a> 插入节点</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//插入节点</span></span><br><span class="line">    <span class="keyword">if</span>(!rt)&#123;<span class="comment">//树中没有一个节点 </span></span><br><span class="line">        rt=++sz;</span><br><span class="line">        key[rt]=x;</span><br><span class="line">        siz[rt]=recy[rt] = <span class="number">1</span>;</span><br><span class="line">        son[rt][<span class="number">0</span>]=son[rt][<span class="number">1</span>]=<span class="number">0</span>;<span class="comment">//赋初值 </span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> now=rt,fa=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(key[now]==x)&#123;<span class="comment">//树中已有此点，重复+1 </span></span><br><span class="line">            ++recy[now];</span><br><span class="line">            update(now);update(fa);</span><br><span class="line">            splay(now);<span class="comment">//splay一下，保证平衡 </span></span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        fa=now,now=son[now][x&gt;key[now]];<span class="comment">//满足二叉查找树的性质，往下跑 </span></span><br><span class="line">        <span class="keyword">if</span>(!now)&#123;</span><br><span class="line">            ++sz;</span><br><span class="line">            key[sz] = x;</span><br><span class="line">            siz[sz] = recy[sz] = <span class="number">1</span>;<span class="comment">//赋初值</span></span><br><span class="line">            f[sz] = fa;<span class="comment">//父亲是fa </span></span><br><span class="line">            son[fa][x &gt; key[fa]] = sz;<span class="comment">//更新父亲的新儿子 </span></span><br><span class="line">            update(fa);<span class="comment">//更新父亲的siz </span></span><br><span class="line">            splay(sz);<span class="comment">//splay一下，保证平衡</span></span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="删除节点-2"><a class="markdownIt-Anchor" href="#删除节点-2"></a> 删除节点</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> no_use=find(x);<span class="comment">//find主要是把当前数的对应点找到，然后旋到根，返回值的排名在这里没用 </span></span><br><span class="line">    <span class="keyword">if</span>(recy[rt]&gt;<span class="number">1</span>)&#123;<span class="comment">//情况1：有重复，重复-1，更新，退出 </span></span><br><span class="line">        --recy[rt];</span><br><span class="line">        update(rt);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//接下来都是没有重复的情况 </span></span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">0</span>]&amp;&amp;!son[rt][<span class="number">1</span>])&#123;<span class="comment">//情况2：没有儿子，直接清空 </span></span><br><span class="line">        clear(rt);</span><br><span class="line">        rt=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">0</span>])&#123;<span class="comment">//情况3：没有左儿子，只有右儿子，右儿子变成根，清除自己 </span></span><br><span class="line">        <span class="keyword">int</span> tmp=rt;</span><br><span class="line">        f[rt=son[rt][<span class="number">1</span>]]=<span class="number">0</span>;</span><br><span class="line">        clear(tmp);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">1</span>])&#123;<span class="comment">//情况4：没有右儿子，只有左儿子，左儿子变成根，清除自己 </span></span><br><span class="line">        <span class="keyword">int</span> tmp=rt;</span><br><span class="line">        f[rt=son[rt][<span class="number">0</span>]]=<span class="number">0</span>;</span><br><span class="line">        clear(tmp);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//情况5：两个儿子都有，这是需要一个很简便的做法</span></span><br><span class="line">	<span class="comment">//把前驱splay到根，保持左子树其他节点不用动</span></span><br><span class="line">	<span class="comment">//原根右儿子变成前驱的右儿子</span></span><br><span class="line">	<span class="comment">//原根功成身退，清除掉</span></span><br><span class="line">	<span class="comment">//最后对前驱的siz进行更新 </span></span><br><span class="line">    <span class="keyword">int</span> tmp=rt,left=pre();</span><br><span class="line">    splay(left);</span><br><span class="line">    connect(son[tmp][<span class="number">1</span>],rt,<span class="number">1</span>);</span><br><span class="line">    clear(tmp);</span><br><span class="line">    update(rt);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="上旋节点"><a class="markdownIt-Anchor" href="#上旋节点"></a> 上旋节点</h4>
<p>上旋操作就是把当前节点向上移动一个深度，所以操作对象是当前节点以及其父节点和子节点</p>
<p><strong>注意：和Treap的旋转节点操作原理一样，但是注意区分操作对象不同</strong></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-4.png" alt></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-5.png" alt></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//上旋x </span></span><br><span class="line">    <span class="keyword">int</span> fa=f[x],ffa=f[fa],m=get(x),n=get(fa);<span class="comment">//确定x，fa的关系 </span></span><br><span class="line">    connect(son[x][m^<span class="number">1</span>],fa,m);<span class="comment">//把要转的儿子转到父亲下，关系为m </span></span><br><span class="line">    connect(fa,x,m^<span class="number">1</span>);<span class="comment">//把父亲转到自己下面，关系为m^1 </span></span><br><span class="line">    connect(x,ffa,n);<span class="comment">//把自己转到父亲的父亲下，关系为n </span></span><br><span class="line">    update(fa),update(x);<span class="comment">//先更新fa，再更新自己，可以自己想想为什么是这个顺序 </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="连接节点"><a class="markdownIt-Anchor" href="#连接节点"></a> 连接节点</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">connect</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y,<span class="keyword">int</span> z)</span></span>&#123;<span class="comment">//x连到y下面，关系为z </span></span><br><span class="line">    <span class="keyword">if</span>(x)f[x]=y;<span class="comment">//存在x，则x的父亲为y </span></span><br><span class="line">    <span class="keyword">if</span>(y)son[y][z]=x;<span class="comment">//存在y，y的z关系儿子为x </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="splay"><a class="markdownIt-Anchor" href="#splay"></a> <code>Splay</code></h4>
<p><code>Splay</code>操作是维持平衡的关键，每一个操作之后都要<code>Splay</code>一下，如果担心就有事没事都<code>Splay</code>一下</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-6.png" alt></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-7.png" alt></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/BT-8.png" alt></p>
<blockquote>
<p>如果不考虑祖父节点、父节点和当前节点的关系随意上旋只会导致平衡树混乱，所以后两种情况两次上旋需要判断三个节点的关系</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">splay</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//将x转到根节点</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> fa;fa=f[x];rotate(x))<span class="comment">//每次总是旋转自己 </span></span><br><span class="line">    <span class="keyword">if</span>(f[fa])rotate(get(x)==get(fa)?fa:x);<span class="comment">//如果有爷爷（父亲的父亲），看父亲与父亲的父亲的关系决定转哪个 </span></span><br><span class="line">    rt=x;<span class="comment">//别忘了，把根赋为当前点 </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询值为v的节点排名-3"><a class="markdownIt-Anchor" href="#查询值为v的节点排名-3"></a> 查询值为<code>v</code>的节点排名</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//查排名</span></span><br><span class="line">    <span class="keyword">int</span> now=rt,ans=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(v&lt;key[now])&#123;</span><br><span class="line">            now=son[now][<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">continue</span>;<span class="comment">//在左子树中 </span></span><br><span class="line">        &#125;</span><br><span class="line">        ans+=siz[son[now][<span class="number">0</span>]];<span class="comment">//排名加上左子树节点个数 </span></span><br><span class="line">        <span class="keyword">if</span>(v==key[now])&#123; </span><br><span class="line">            splay(now);</span><br><span class="line">            <span class="keyword">return</span> ans+<span class="number">1</span>; </span><br><span class="line">        &#125;<span class="comment">//值等于当前点，splay一下，保证平衡，排名+1为当前排名 </span></span><br><span class="line">        ans+=recy[now];<span class="comment">//排名加上当前节点的数的个数 </span></span><br><span class="line">        now=son[now][<span class="number">1</span>];<span class="comment">//在右子树中 </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询排名为k的节点值-3"><a class="markdownIt-Anchor" href="#查询排名为k的节点值-3"></a> 查询排名为<code>k</code>的节点值</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kth</span><span class="params">(<span class="keyword">int</span> k)</span></span>&#123;<span class="comment">//查找排名为k的数</span></span><br><span class="line">    <span class="keyword">int</span> now=rt;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(son[now][<span class="number">0</span>]&amp;&amp;k&lt;=siz[son[now][<span class="number">0</span>]])&#123;<span class="comment">//在左子树中 </span></span><br><span class="line">            now=son[now][<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(son[now][<span class="number">0</span>])k-=siz[son[now][<span class="number">0</span>]];<span class="comment">//存在左儿子，排名减去左子树节点数 </span></span><br><span class="line">        <span class="keyword">if</span>(k&lt;=recy[now])&#123; </span><br><span class="line">            splay(now); </span><br><span class="line">            <span class="keyword">return</span> key[now]; </span><br><span class="line">        &#125;<span class="comment">//说明就是当前点，splay一下，保证平衡，退出 </span></span><br><span class="line">        k-=recy[now];<span class="comment">//排名减去当前节点数的个数 </span></span><br><span class="line">        now=son[now][<span class="number">1</span>];<span class="comment">//在右子树中 </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="查询根节点的前去后继节点"><a class="markdownIt-Anchor" href="#查询根节点的前去后继节点"></a> 查询根节点的前去后继节点</h4>
<p><strong>注意：查询的结果是节点，要是想知道节点值要外套<code>key[]</code></strong></p>
<blockquote>
<p>那么如何查询某一个节点的前驱后继节点呢？</p>
<p>因为每一个操作之后都会执行<code>Splay</code>使节点到达根节点，那么可以通过先插入要查询的节点使其到达根节点，然后在调用当前函数，最后在删除插入的节点来实现</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pre</span><span class="params">()</span></span>&#123;<span class="comment">//前驱为左子树中最大的那个 </span></span><br><span class="line">    <span class="keyword">int</span> now=son[rt][<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">while</span>(son[now][<span class="number">1</span>])now=son[now][<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">return</span> now;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">nxt</span><span class="params">()</span></span>&#123;<span class="comment">//后继为右子树中最小的那个 </span></span><br><span class="line">    <span class="keyword">int</span> now=son[rt][<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">while</span>(son[now][<span class="number">0</span>])now=son[now][<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">return</span> now;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="板子总结-3"><a class="markdownIt-Anchor" href="#板子总结-3"></a> 板子总结</h4>
<blockquote>
<p>这个板子没有采用数组统计存入数据再递归建树，而是直接插入</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> N=<span class="number">100010</span>;</span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> sz;<span class="comment">//表示当前节点个数</span></span><br><span class="line"><span class="keyword">int</span> rt;<span class="comment">//表示当前根节点的编号</span></span><br><span class="line"><span class="keyword">int</span> f[N];<span class="comment">//表示编号x节点的父亲的编号</span></span><br><span class="line"><span class="keyword">int</span> key[N];<span class="comment">//表示编号为x的节点的对应的数值</span></span><br><span class="line"><span class="keyword">int</span> siz[N];<span class="comment">//表示以编号为x的节点为根的子树的节点个数</span></span><br><span class="line"><span class="keyword">int</span> recy[N];<span class="comment">//表示以编号为x的节点的数值重复次数</span></span><br><span class="line"><span class="keyword">int</span> son[N][<span class="number">2</span>];</span><br><span class="line"><span class="comment">//son[x][0]表示以编号为x的节点的左儿子的编号</span></span><br><span class="line"><span class="comment">//son[x][1]表示以编号为x的节点的右儿子的编号</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">()</span></span>&#123;<span class="comment">//快读</span></span><br><span class="line">    <span class="keyword">int</span> s=<span class="number">0</span>,w =<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">char</span> c=getchar();</span><br><span class="line">    <span class="keyword">for</span> (;!<span class="built_in">isdigit</span>(c);c=getchar())<span class="keyword">if</span>(c==<span class="string">'-'</span>)w=<span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span> (;<span class="built_in">isdigit</span>(c);c=getchar())s=(s&lt;&lt;<span class="number">1</span>)+(s&lt;&lt;<span class="number">3</span>)+(c^<span class="number">48</span>);</span><br><span class="line">    <span class="keyword">return</span> s*w;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//清除</span></span><br><span class="line">    f[x]=key[x]=siz[x]=recy[x]=son[x][<span class="number">0</span>]=son[x][<span class="number">1</span>]=<span class="number">0</span>;</span><br><span class="line">	<span class="comment">//5个数组全部清零</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//判断节点是父节点的左右孩子</span></span><br><span class="line">    <span class="keyword">return</span> son[f[x]][<span class="number">1</span>]==x;<span class="comment">//如果自己是父亲的右儿子，返回1；否则返回0（0为左儿子，1为右儿子） </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//更新节点参数</span></span><br><span class="line">    <span class="keyword">if</span>(x)&#123;<span class="comment">//如果这个点存在</span></span><br><span class="line">        siz[x]=recy[x];<span class="comment">//自己重复的次数先累计</span></span><br><span class="line">        <span class="keyword">if</span>(son[x][<span class="number">0</span>])siz[x]+=siz[son[x][<span class="number">0</span>]];</span><br><span class="line">        <span class="keyword">if</span>(son[x][<span class="number">1</span>])siz[x]+=siz[son[x][<span class="number">1</span>]];</span><br><span class="line">		<span class="comment">//如果存在儿子，把儿子的siz累积到自己</span></span><br><span class="line">		<span class="comment">//然后发现一个问题，更新自己的siz时，必须保证儿子的siz是正确的</span></span><br><span class="line">		<span class="comment">//所以后面的操作，当牵扯到儿子和父亲时，应该先更新新儿子，后更新新父亲</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">connect</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y,<span class="keyword">int</span> z)</span></span>&#123;<span class="comment">//x连到y下面，关系为z </span></span><br><span class="line">    <span class="keyword">if</span>(x)f[x]=y;<span class="comment">//存在x，则x的父亲为y </span></span><br><span class="line">    <span class="keyword">if</span>(y)son[y][z]=x;<span class="comment">//存在y，y的z关系儿子为x </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//上旋x </span></span><br><span class="line">    <span class="keyword">int</span> fa=f[x],ffa=f[fa],m=get(x),n=get(fa);<span class="comment">//确定x，fa的关系 </span></span><br><span class="line">    connect(son[x][m^<span class="number">1</span>],fa,m);<span class="comment">//把要转的儿子转到父亲下，关系为m </span></span><br><span class="line">    connect(fa,x,m^<span class="number">1</span>);<span class="comment">//把父亲转到自己下面，关系为m^1 </span></span><br><span class="line">    connect(x,ffa,n);<span class="comment">//把自己转到父亲的父亲下，关系为n </span></span><br><span class="line">    update(fa),update(x);<span class="comment">//先更新fa，再更新自己，可以自己想想为什么是这个顺序（因为fa此时变成了x的子节点，要自下而上更新） </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">splay</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//将x转到根节点</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> fa;fa=f[x];rotate(x))<span class="comment">//每次总是旋转自己 </span></span><br><span class="line">    <span class="keyword">if</span>(f[fa])rotate(get(x)==get(fa)?fa:x);<span class="comment">//如果有爷爷（父亲的父亲），看父亲与父亲的父亲的关系决定转哪个 </span></span><br><span class="line">    rt=x;<span class="comment">//别忘了，把根赋为当前点 </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//插入节点</span></span><br><span class="line">    <span class="keyword">if</span>(!rt)&#123;<span class="comment">//树中没有一个节点 </span></span><br><span class="line">        rt=++sz;</span><br><span class="line">        key[rt]=x;</span><br><span class="line">        siz[rt]=recy[rt] = <span class="number">1</span>;</span><br><span class="line">        son[rt][<span class="number">0</span>]=son[rt][<span class="number">1</span>]=<span class="number">0</span>;<span class="comment">//赋初值 </span></span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> now=rt,fa=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(key[now]==x)&#123;<span class="comment">//树中已有此点，重复+1 </span></span><br><span class="line">            ++recy[now];</span><br><span class="line">            update(now);update(fa);</span><br><span class="line">            splay(now);<span class="comment">//splay一下，保证平衡 </span></span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        fa=now,now=son[now][x&gt;key[now]];<span class="comment">//满足二叉查找树的性质，往下跑 </span></span><br><span class="line">        <span class="keyword">if</span>(!now)&#123;</span><br><span class="line">            ++sz;</span><br><span class="line">            key[sz] = x;</span><br><span class="line">            siz[sz] = recy[sz] = <span class="number">1</span>;<span class="comment">//赋初值</span></span><br><span class="line">            f[sz] = fa;<span class="comment">//父亲是fa </span></span><br><span class="line">            son[fa][x &gt; key[fa]] = sz;<span class="comment">//更新父亲的新儿子 </span></span><br><span class="line">            update(fa);<span class="comment">//更新父亲的siz </span></span><br><span class="line">            splay(sz);<span class="comment">//splay一下，保证平衡</span></span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> v)</span></span>&#123;<span class="comment">//查排名</span></span><br><span class="line">    <span class="keyword">int</span> now=rt,ans=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(v&lt;key[now])&#123;</span><br><span class="line">            now=son[now][<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">continue</span>;<span class="comment">//在左子树中 </span></span><br><span class="line">        &#125;</span><br><span class="line">        ans+=siz[son[now][<span class="number">0</span>]];<span class="comment">//排名加上左子树节点个数 </span></span><br><span class="line">        <span class="keyword">if</span>(v==key[now])&#123; </span><br><span class="line">            splay(now);</span><br><span class="line">            <span class="keyword">return</span> ans+<span class="number">1</span>; </span><br><span class="line">        &#125;<span class="comment">//值等于当前点，splay一下，保证平衡，排名+1为当前排名 </span></span><br><span class="line">        ans+=recy[now];<span class="comment">//排名加上当前节点的数的个数 </span></span><br><span class="line">        now=son[now][<span class="number">1</span>];<span class="comment">//在右子树中 </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kth</span><span class="params">(<span class="keyword">int</span> k)</span></span>&#123;<span class="comment">//查找排名为k的数</span></span><br><span class="line">    <span class="keyword">int</span> now=rt;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(son[now][<span class="number">0</span>]&amp;&amp;k&lt;=siz[son[now][<span class="number">0</span>]])&#123;<span class="comment">//在左子树中 </span></span><br><span class="line">            now=son[now][<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(son[now][<span class="number">0</span>])k-=siz[son[now][<span class="number">0</span>]];<span class="comment">//存在左儿子，排名减去左子树节点数 </span></span><br><span class="line">        <span class="keyword">if</span>(k&lt;=recy[now])&#123; </span><br><span class="line">            splay(now); </span><br><span class="line">            <span class="keyword">return</span> key[now]; </span><br><span class="line">        &#125;<span class="comment">//说明就是当前点，splay一下，保证平衡，退出 </span></span><br><span class="line">        k-=recy[now];<span class="comment">//排名减去当前节点数的个数 </span></span><br><span class="line">        now=son[now][<span class="number">1</span>];<span class="comment">//在右子树中 </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pre</span><span class="params">()</span></span>&#123;<span class="comment">//前驱为左子树中最大的那个 </span></span><br><span class="line">    <span class="keyword">int</span> now=son[rt][<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">while</span>(son[now][<span class="number">1</span>])now=son[now][<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">return</span> now;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">nxt</span><span class="params">()</span></span>&#123;<span class="comment">//后继为右子树中最小的那个 </span></span><br><span class="line">    <span class="keyword">int</span> now=son[rt][<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">while</span>(son[now][<span class="number">0</span>])now=son[now][<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">return</span> now;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">del</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> no_use=find(x);<span class="comment">//find主要是把当前数的对应点找到，然后旋到根，返回值的排名在这里没用 </span></span><br><span class="line">    <span class="keyword">if</span>(recy[rt]&gt;<span class="number">1</span>)&#123;<span class="comment">//情况1：有重复，重复-1，更新，退出 </span></span><br><span class="line">        --recy[rt];</span><br><span class="line">        update(rt);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//接下来都是没有重复的情况 </span></span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">0</span>]&amp;&amp;!son[rt][<span class="number">1</span>])&#123;<span class="comment">//情况2：没有儿子，直接清空 </span></span><br><span class="line">        clear(rt);</span><br><span class="line">        rt=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">0</span>])&#123;<span class="comment">//情况3：没有左儿子，只有右儿子，右儿子变成根，清除自己 </span></span><br><span class="line">        <span class="keyword">int</span> tmp=rt;</span><br><span class="line">        f[rt=son[rt][<span class="number">1</span>]]=<span class="number">0</span>;</span><br><span class="line">        clear(tmp);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(!son[rt][<span class="number">1</span>])&#123;<span class="comment">//情况4：没有右儿子，只有左儿子，左儿子变成根，清除自己 </span></span><br><span class="line">        <span class="keyword">int</span> tmp=rt;</span><br><span class="line">        f[rt=son[rt][<span class="number">0</span>]]=<span class="number">0</span>;</span><br><span class="line">        clear(tmp);</span><br><span class="line">        <span class="keyword">return</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//情况5：两个儿子都有，这是需要一个很简便的做法</span></span><br><span class="line">	<span class="comment">//把前驱splay到根，保持左子树其他节点不用动</span></span><br><span class="line">	<span class="comment">//原根右儿子变成前驱的右儿子</span></span><br><span class="line">	<span class="comment">//原根功成身退，清除掉</span></span><br><span class="line">	<span class="comment">//最后对前驱的siz进行更新 </span></span><br><span class="line">    <span class="keyword">int</span> tmp=rt,left=pre();</span><br><span class="line">    splay(left);</span><br><span class="line">    connect(son[tmp][<span class="number">1</span>],rt,<span class="number">1</span>);</span><br><span class="line">    clear(tmp);</span><br><span class="line">    update(rt);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> t=read();</span><br><span class="line">    <span class="keyword">while</span>(t--)&#123;</span><br><span class="line">        <span class="keyword">int</span> opt=read(),x=read();</span><br><span class="line">        <span class="keyword">if</span>(opt==<span class="number">1</span>)insert(x);</span><br><span class="line">        <span class="keyword">if</span>(opt==<span class="number">2</span>)del(x);</span><br><span class="line">        <span class="keyword">if</span>(opt==<span class="number">3</span>)<span class="built_in">printf</span>(<span class="string">"%d\n"</span>,find(x));</span><br><span class="line">        <span class="keyword">if</span>(opt==<span class="number">4</span>) <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,kth(x));</span><br><span class="line">        <span class="keyword">if</span>(opt==<span class="number">5</span>)&#123;</span><br><span class="line">            insert(x);</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,key[pre()]);</span><br><span class="line">            del(x);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(opt == <span class="number">6</span>)&#123;</span><br><span class="line">            insert(x);</span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">"%d\n"</span>,key[nxt()]);</span><br><span class="line">            del(x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="拓展操作"><a class="markdownIt-Anchor" href="#拓展操作"></a> 拓展操作</h3>
<blockquote>
<p>其实上述的基本操作<code>Splay</code>树操作<code>Treap</code>树也完全可以，但是<code>Splay</code>树的强大远远没有体现出来，否则他它也不会有“序列之王”的称号了，<code>Splay</code>树因为上旋和<code>Splay</code>可以实现对序列的区间翻转、移动等操作</p>
</blockquote>
<p><strong>首先无疑的是伸展树的中序遍历可以按照顺序输出序列，很重要的一个操作是如何在伸展树中表示任意一个区间？</strong></p>
<p>如果要提取<code>[a,b]</code>区间，那么将<code>a</code>前一个节点转到根节点，将<code>b</code>的后一个节点转到根节点的右边，则根节点的左子树就是我们想要的<code>[a,b]</code>区间</p>
<p>利用上述功能我们可以实现线段树所有的功能：在每个节点记录关于这个节点和以该节点为根的子树的信息，然后询问时先提取区间，在读取子树的信息。同时还可以对区间整体进行修改，利用类似线段树的“懒操作”标记，下次查询的时候如果发现标记就将标记释放到子节点然后向下查询子节点</p>
<p><strong>除了实现线段树区间功能，<code>Splay</code>树还可以实现线段树无法实现的操作：</strong></p>
<ul>
<li>在<code>a</code>节点后面插入一数：先把要插入的数建成一个<code>Splay</code>树然后将<code>a</code>节点转到根节点并把<code>a</code>节点的后一个节点转到根节点的右边，最后将新建的这个树挂到根节点的右子节点的左子上</li>
<li>删除一个区间<code>[a,b]</code>的数：先提取出<code>[a,b]</code>区间然后直接删除</li>
<li>翻转区间<code>[a,b]</code>：先取出<code>[a,b]</code>区间然后通过“懒标记”记录，查询时存在标记就翻转当前节点左右子节点并下传标记</li>
</ul>
<p><strong>注意</strong>：没进行一次对序列的修改操作之后都要维护一下<code>Splay</code>树，即对受到影响的节点的参数进行更新，一种方法是对影响到的点自下而上进行更新操作，挥着直接将修改的节点旋转到根节点，因为旋转操作的同时会维护每个节点的值。另外一个问题是如果序列的第一个数字之前没有数字了，并且最后一个数字后边也没有数字了，此时再提取区间就会出现一些问题。为了防止进行过多的特殊判断，在序列的最前面和最后面分别放一个不影响操作结果的数（一般是负无穷和正无穷），在<code>Splay</code>树中就体现为两个节点。如此提取区间是原来的第<code>k</code>个数就变成了第<code>k+1</code>个数。</p>
<p><a href="https://www.luogu.com.cn/problem/P3391" target="_blank" rel="noopener">模板题：文艺平衡树</a>（翻转指定区间）</p>
<blockquote>
<p>因为题不涉及插入节点，所以采用数组先存数据再递归建树</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> inf 0x3f3f3f3f</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 100005<span class="comment">//数组不用加倍 因为平衡树一个点对应一个点 而不是一段区间 </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> mid ((l+r)&gt;&gt;1)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> sum=<span class="number">0</span>,root=<span class="number">0</span>;<span class="comment">//寄存指针和根节点</span></span><br><span class="line"><span class="keyword">int</span> siz[N];</span><br><span class="line"><span class="keyword">int</span> key[N];<span class="comment">//表示以编号为x的节点为根的子树的节点个数</span></span><br><span class="line"><span class="keyword">int</span> lazy[N];</span><br><span class="line"><span class="keyword">int</span> a[N];<span class="comment">//</span></span><br><span class="line"><span class="keyword">int</span> fa[N];</span><br><span class="line"><span class="keyword">int</span> son[N][<span class="number">3</span>];</span><br><span class="line"><span class="comment">//son[x][0]表示以编号为x的节点的左儿子的编号</span></span><br><span class="line"><span class="comment">//son[x][1]表示以编号为x的节点的右儿子的编号</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">int</span> s)</span></span>&#123;<span class="comment">//更新参数</span></span><br><span class="line">     siz[s]=siz[son[s][<span class="number">0</span>]]+siz[son[s][<span class="number">1</span>]]+<span class="number">1</span>; </span><br><span class="line">&#125;<span class="comment">//+1因为有它自己 </span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">build</span><span class="params">(<span class="keyword">int</span> f,<span class="keyword">int</span> l,<span class="keyword">int</span> r)</span></span>&#123;<span class="comment">//建树</span></span><br><span class="line">    <span class="keyword">if</span>(l&gt;r) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> s=++sum;</span><br><span class="line">    key[s]=a[mid]; fa[s]=f; lazy[s]=<span class="number">0</span>;</span><br><span class="line">    son[s][<span class="number">0</span>]=build(s,l,mid<span class="number">-1</span>);<span class="comment">//注意与线段树的差异：s记录的是mid这个点 而它的左右儿子分别记录l~mid-1 mid+1~r </span></span><br><span class="line">    son[s][<span class="number">1</span>]=build(s,mid+<span class="number">1</span>,r);</span><br><span class="line">    update(s);</span><br><span class="line">    <span class="keyword">return</span> s;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123; <span class="comment">//判断自己是父节点的左右孩子</span></span><br><span class="line">    <span class="keyword">return</span> (son[fa[x]][<span class="number">1</span>]==x) ; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">pushdown</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//下传翻转标记</span></span><br><span class="line">    <span class="keyword">if</span>(x&amp;&amp;lazy[x])&#123;</span><br><span class="line">        lazy[son[x][<span class="number">0</span>]]^=<span class="number">1</span>;<span class="comment">//向左右儿子传标记 </span></span><br><span class="line">        lazy[son[x][<span class="number">1</span>]]^=<span class="number">1</span>;</span><br><span class="line">        swap(son[x][<span class="number">0</span>],son[x][<span class="number">1</span>]);<span class="comment">//区间翻转 </span></span><br><span class="line">        lazy[x]=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//上旋</span></span><br><span class="line">    <span class="keyword">int</span> old=fa[x],oldf=fa[old],whison=get(x);</span><br><span class="line">    son[old][whison]=son[x][whison^<span class="number">1</span>];</span><br><span class="line">    fa[son[old][whison]]=old;</span><br><span class="line">    fa[old]=x; son[x][whison^<span class="number">1</span>]=old;</span><br><span class="line">    fa[x]=oldf;</span><br><span class="line">    <span class="keyword">if</span>(oldf) son[oldf][son[oldf][<span class="number">1</span>]==old]=x;</span><br><span class="line">    update(old),update(x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">splay</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> tt)</span></span>&#123;<span class="comment">//将x旋到tt的右孩子节点</span></span><br><span class="line">    <span class="comment">//其实这个操作旋了两次 一次是for结束时 一次是if判断后 </span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> f;(f=fa[x])!=tt;rotate(x))</span><br><span class="line">    <span class="comment">//判断旋两次后能不能到tt的儿子节点 如果能到 就不用旋了 直接通过for循环来旋 </span></span><br><span class="line">    <span class="keyword">if</span>(fa[fa[x]]!=tt) rotate(get(x)==get(f)?f:x);<span class="comment">//如果在一条直线 就要先旋fa </span></span><br><span class="line">    <span class="keyword">if</span>(!tt) root=x;  </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get_rank</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;<span class="comment">//查询节点的排名</span></span><br><span class="line">    <span class="keyword">int</span> now=root;</span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        pushdown(now);<span class="comment">//查找排名时记得下传标记 </span></span><br><span class="line">        <span class="keyword">if</span>(x&lt;=siz[son[now][<span class="number">0</span>]]) now=son[now][<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">else</span>&#123;</span><br><span class="line">            x=x-siz[son[now][<span class="number">0</span>]]<span class="number">-1</span>;<span class="comment">//与线段树不同的是 它不仅要减去儿子节点 还要减去它自己 因为它自己存了mid这个点 </span></span><br><span class="line">            <span class="keyword">if</span>(!x) <span class="keyword">return</span> now;</span><br><span class="line">            now=son[now][<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">turn</span><span class="params">(<span class="keyword">int</span> l,<span class="keyword">int</span> r)</span></span>&#123;<span class="comment">//翻转[l,r]区间</span></span><br><span class="line">    <span class="keyword">int</span> x=get_rank(l),y=get_rank(r+<span class="number">2</span>);<span class="comment">//因为维护的是值域平衡树 所以要先找到在平衡树中的排位(也就是说对应着哪个点) </span></span><br><span class="line">    splay(x,<span class="number">0</span>); splay(y,x);<span class="comment">//把x旋到根节点(0是因为根的父亲是0) 把y旋到x的右节点 </span></span><br><span class="line">    lazy[son[son[root][<span class="number">1</span>]][<span class="number">0</span>]]^=<span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> now)</span></span>&#123;</span><br><span class="line">    pushdown(now);</span><br><span class="line">    <span class="keyword">if</span>(son[now][<span class="number">0</span>]) dfs(son[now][<span class="number">0</span>]);</span><br><span class="line">    <span class="keyword">if</span>(key[now]!=-inf&amp;&amp;key[now]!=inf) <span class="built_in">printf</span>(<span class="string">"%d "</span>,key[now]);<span class="comment">//注意是key里面存储真正的值 而now只是平衡树的节点 </span></span><br><span class="line">    <span class="keyword">if</span>(son[now][<span class="number">1</span>]) dfs(son[now][<span class="number">1</span>]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n,m;</span><br><span class="line">    <span class="comment">//scanf("%d%d",&amp;n,&amp;m);//n个数据,m次操作</span></span><br><span class="line">    <span class="built_in">cin</span>&gt;&gt;n&gt;&gt;m;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;=n+<span class="number">1</span>;i++) </span><br><span class="line">    a[i]=i<span class="number">-1</span>;<span class="comment">//从2开始存入n个数</span></span><br><span class="line">    a[<span class="number">1</span>]=-inf; a[n+<span class="number">2</span>]=inf;<span class="comment">//为了防止区间翻转特殊判断分别加上正负无穷</span></span><br><span class="line">    root=build(<span class="number">0</span>,<span class="number">1</span>,n+<span class="number">2</span>);<span class="comment">//根是0,注意是1到 n+2因为加了正无穷和负无穷</span></span><br><span class="line">    <span class="keyword">int</span> l,r; </span><br><span class="line">    <span class="keyword">while</span>(m--)&#123;</span><br><span class="line">        <span class="comment">//scanf("%d%d",&amp;l,&amp;r);</span></span><br><span class="line">        <span class="built_in">cin</span>&gt;&gt;l&gt;&gt;r;</span><br><span class="line">        turn(l,r);<span class="comment">//翻转操作</span></span><br><span class="line">    &#125;</span><br><span class="line">    dfs(root);<span class="comment">//中序遍历输出结果</span></span><br><span class="line">    system(<span class="string">"pause"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="对比总结"><a class="markdownIt-Anchor" href="#对比总结"></a> 对比总结</h2>
<p>以目前学习的阶段来看<code>Treap</code>树和<code>Splay</code>树的差异不大，代码复杂程度和效率都不相上下。但是<code>Splay</code>不需要额外的随机域，并且<code>Splay</code>的区间操作有时比较方便（虽然<code>Treap</code>树也可以强行区间修改但是失去了本身随机域维持期望平衡的意义）</p>
]]></content>
      <categories>
        <category>ACM学习</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>算法与数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>常见排序总结</title>
    <url>/2020/07/19/%E5%B8%B8%E8%A7%81%E6%8E%92%E5%BA%8F%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="插入排序"><a class="markdownIt-Anchor" href="#插入排序"></a> 插入排序</h1>
<p>插入排序适合对<mark>少量数据</mark>的排序</p>
<p><strong>基本思想</strong>：每次处理就是将无序的数列中第一个元素与有序数列的元素从后到前比较，找到插入位置，将该元素插入到有序数列的适当的最终的位置上，是一种简单直观的排序算法</p>
<p><strong>默认插入排序的初始数组从第1位开始存入</strong></p>
<h2 id="直接插入排序"><a class="markdownIt-Anchor" href="#直接插入排序"></a> 直接插入排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：把待排序的记录按其关键码值的大小逐个插入到一个已经排好序的有序序列中，直到所有的记录插入完为止，得到一个新的有序序列</p>
<blockquote>
<p>因为如果只有一个数的话是不用排序的，所以<code>n</code>个数只用给<code>n-1</code>个数找位置，所以总共需要循环<code>n-1</code>次。每一次循环过后<mark>有序序列变长，无序序列变短</mark></p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//在插入排序中一般从第一个位置存值，第0位用来存正在拿来比较的值（哨兵位）</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;=n;i++)&#123;<span class="comment">//第一个不用排序</span></span><br><span class="line">	<span class="keyword">if</span>(arr[i]&lt;=arr[i<span class="number">-1</span>])&#123;</span><br><span class="line">        arr[<span class="number">0</span>]=arr[i];<span class="comment">//哨兵位赋值</span></span><br><span class="line">        <span class="keyword">int</span> j;</span><br><span class="line">        <span class="keyword">for</span>(j=i<span class="number">-1</span>;arr[<span class="number">0</span>]&lt;arr[j];--j)<span class="comment">//记录后移arr[0]&lt;arr[j] </span></span><br><span class="line">        arr[j+<span class="number">1</span>]=arr[j];</span><br><span class="line">        arr[j+<span class="number">1</span>]=arr[<span class="number">0</span>];<span class="comment">//用哨兵位赋值</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="折半排序"><a class="markdownIt-Anchor" href="#折半排序"></a> 折半排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：在直接插入排序基础上将遍历查找插入位置改为<mark>二分查找</mark>来加快速度</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;=n;i++)&#123;</span><br><span class="line">	arr[<span class="number">0</span>]=arr[i];</span><br><span class="line">	<span class="keyword">int</span> low=<span class="number">1</span>,high=i<span class="number">-1</span>,mid;</span><br><span class="line">	<span class="keyword">while</span>(low&lt;=high)&#123;</span><br><span class="line">		mid=(low+high)/<span class="number">2</span>;</span><br><span class="line">		<span class="keyword">if</span>(arr[i]&lt;arr[mid])high=mid<span class="number">-1</span>;</span><br><span class="line">		<span class="keyword">else</span> low=mid+<span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> j=i<span class="number">-1</span>;j&gt;=high;--j)</span><br><span class="line">	arr[j+<span class="number">1</span>]=arr[j];</span><br><span class="line">	arr[high+<span class="number">1</span>]=arr[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="二路排序"><a class="markdownIt-Anchor" href="#二路排序"></a> 二路排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(n)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：在折半排序的基础上进一步改进，将排序由沿一个方向上移动改为沿两个方向上移动，如此进一步减少了移动次数</p>
<blockquote>
<p>我们设定一个辅助数组<code>A</code>，大小为原数组+1，将<code>A[0]</code>设为原数组的第一个数，通过<code>first</code>和<code>last</code>指针指向有序序列的最小值和最大值（即头部和尾部）并且利用<a href="https://blog.csdn.net/taoqick/article/details/19759717?depth_1-utm_source=distribute.pc_relevant.none-task&amp;utm_source=distribute.pc_relevant.none-task" target="_blank" rel="noopener">约瑟夫环</a>使其成为一个循环数组进行<mark>两端插入</mark></p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//此算法不同于上述算法，多开了一个辅助数组，因此也不需要哨兵位，从0开始存值 </span></span><br><span class="line"><span class="keyword">int</span> T[n+<span class="number">1</span>];</span><br><span class="line">T[<span class="number">0</span>]=arr[<span class="number">1</span>];<span class="comment">//第一个数放入辅助数组</span></span><br><span class="line"><span class="keyword">int</span> first,last;</span><br><span class="line">first=last=<span class="number">0</span>;<span class="comment">//初始都指向辅助数组第0位</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">2</span>;i&lt;=n;i++)&#123;</span><br><span class="line">	<span class="keyword">if</span>(arr[i]&lt;T[first])&#123;<span class="comment">//头前插入</span></span><br><span class="line">		first=(first<span class="number">-1</span>+n)%n;</span><br><span class="line">		T[first]=arr[i];</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">if</span>(arr[i]&gt;T[last])&#123;<span class="comment">//尾后插入</span></span><br><span class="line">		last++;</span><br><span class="line">		T[last]=arr[i];</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">else</span>&#123;<span class="comment">//在有序序列之间插入</span></span><br><span class="line">		last++;</span><br><span class="line">		T[last]=T[last<span class="number">-1</span>];</span><br><span class="line">		<span class="keyword">int</span> j;</span><br><span class="line">		<span class="keyword">for</span>(j=last<span class="number">-1</span>;arr[i]&lt;T[(j<span class="number">-1</span>)%n];j=(j<span class="number">-1</span>+n)%n)</span><br><span class="line">		arr[j]=T[(j<span class="number">-1</span>+n)%n];</span><br><span class="line">		T[j]=arr[i];</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;<span class="comment">//将辅助数组T记录的有序列表转给原数组</span></span><br><span class="line">	arr[i+<span class="number">1</span>]=T[first];</span><br><span class="line">	first=(first+<span class="number">1</span>)%n;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="希尔排序"><a class="markdownIt-Anchor" href="#希尔排序"></a> 希尔排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>1.3</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>不稳定</td>
</tr>
</tbody>
</table>
<p><strong>引入</strong>：希尔排序是直接插入排序另一种更改思路，插入排序在对几乎已经排好序的数列操作时，效率高（即可以达到线性排序的效率）。但插入排序一般是低效的，因为插入排序每次只能将数据移动一位，所以希尔排序不再加快查找效率而是<mark>加快移动效率</mark>，通过设置不同增量可以让特别靠后的小数向前“跳”着到序列前面，又称“缩小增量排序”</p>
<p><strong>思想</strong>：希尔排序是把记录按下标的一定增量分组，对每组使用直接插入排序算法排序。随着增量逐渐减少，每组包含的关键词越来越多，当增量减至1时，整个数列恰被分成一组形成有序序列</p>
<p><strong>意义</strong>：希尔排序是第一个将排序的时间复杂度降至O(n<sup>2</sup>)以下的，该方法实质上是一种分组插入方法</p>
<p><strong>注意</strong>：希尔排序相当于是根据不同增量进行宏观调，控但如此也变得不稳定了</p>
<blockquote>
<p>增量分组没有特定的科学依据，比较玄学，但是分的好也就快点，<code>D.L.Shell</code>建议是减半分组，例如：8，4，2，1，反正最后都是要进行一次间隔1的希尔插入</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> t=<span class="number">4</span>;<span class="comment">//可以自己根据情况来改的</span></span><br><span class="line"><span class="keyword">int</span> dlta[t]=&#123;<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ShellInsert</span><span class="params">(<span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=k+<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(arr[i]&lt;arr[i-k])&#123;</span><br><span class="line">            arr[<span class="number">0</span>]=arr[i];</span><br><span class="line">            <span class="keyword">int</span> j;</span><br><span class="line">            <span class="keyword">for</span>(j=i-k;j&gt;<span class="number">0</span>&amp;&amp;arr[<span class="number">0</span>]&lt;arr[j];j-=k)</span><br><span class="line">            arr[j+k]=arr[j];</span><br><span class="line">            arr[j+k]=arr[<span class="number">0</span>];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ShellSort</span><span class="params">(<span class="keyword">int</span> dlta[],<span class="keyword">int</span> t)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">0</span>;k&lt;t;k++)</span><br><span class="line">    ShellInsert(dlta[k]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="交换排序"><a class="markdownIt-Anchor" href="#交换排序"></a> 交换排序</h1>
<p>交换排序也是只适合对<mark>少量数据</mark>的排序</p>
<p><strong>基本思想</strong>：所谓交换就是根据序列中两个记录键值的比较结果来对换这两个记录在序列中的位置，每次交换将键值较大的记录向序列的尾部移动，键值较小的记录向序列的前部移动</p>
<p><strong>默认交换排序的初始数组从第1位开始存入，第0位仍作为哨兵位用于交换操作：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;<span class="comment">//这里定义swap不是STL函数而是手写模拟过程</span></span><br><span class="line">    arr[<span class="number">0</span>]=arr[x];</span><br><span class="line">    arr[x]=arr[y];</span><br><span class="line">    arr[y]=arr[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="冒泡排序"><a class="markdownIt-Anchor" href="#冒泡排序"></a> 冒泡排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：利用的比较交换，利用循环将第<code>i</code>小或者大的元素归位，归位操作利用的是对<code>n</code>个元素中相邻的两个进行比较，如果顺序正确就不交换，如果顺序错误就进行位置的交换。通过重复的循环访问数组，直到没有可以交换的元素</p>
<blockquote>
<p>正如名字那样冒泡排序就好像冒水泡一样第<code>i</code>次循环通过交换过程使<code>i</code>小（大）的元素由序列一端浮向另一端，如果有n个数要排序那就要循环n-1次</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;n;i++)&#123;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;=n-i;j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[j]&gt;arr[j+<span class="number">1</span>])&#123;</span><br><span class="line">			swap(j,j+<span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="冒泡排序优化"><a class="markdownIt-Anchor" href="#冒泡排序优化"></a> 冒泡排序优化</h2>
<h3 id="优化一"><a class="markdownIt-Anchor" href="#优化一"></a> 优化一</h3>
<p><strong>引入</strong>：假设我们现在排序<code>arr[]={1,2,3,4,5,6,7,8,10,9}</code>这组数据，按照上面的排序方式，第一趟排序后将10和9交换已经有序，接下来的8趟排序就是多余的，什么也没做</p>
<p><strong>思想</strong>：我们可以在<mark>交换的地方加一个标记</mark>，如果那一趟排序没有交换元素，说明这组数据已经有序，不用再继续下去</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> flag=<span class="literal">true</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;n&amp;&amp;flag;i++)&#123;</span><br><span class="line">	flag=<span class="literal">false</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;n;j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[j]&gt;arr[j+<span class="number">1</span>])&#123;</span><br><span class="line">			swap(j,j+<span class="number">1</span>);</span><br><span class="line">			flag=<span class="literal">true</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="优化二"><a class="markdownIt-Anchor" href="#优化二"></a> 优化二</h3>
<p><strong>引入</strong>：优化一仅仅适用于连片有序而整体无序的数据（例如：1， 2，3 ，4 ，7，6，5）。但是对于前面大部分是无序而后边小半部分有序的数据（1，2，5，7，4，3，6，8，9，10）排序效率也不可观，对于种类型数据，我们可以继续优化</p>
<p><strong>思想</strong>：我们可以<mark>记下最后一次交换的位置</mark>，后边没有交换，必然是有序的，然后下一次排序从第一个比较到上次记录的位置结束即可</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> flag=n;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=flag+<span class="number">1</span>;i++)&#123;</span><br><span class="line">	<span class="keyword">int</span> k=flag;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">1</span>;j&lt;k;j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[j]&gt;arr[j+<span class="number">1</span>])&#123;</span><br><span class="line">			flag=j+<span class="number">1</span>;</span><br><span class="line">			swap(j,j+<span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="鸡尾酒排序"><a class="markdownIt-Anchor" href="#鸡尾酒排序"></a> 鸡尾酒排序</h2>
<p><strong>引入</strong>：上述冒泡排序每次只能找出一个最大（小）值，其实好可以继续优化代码减少循环次数，鸡尾酒排序其实相当于冒泡排序的优化三</p>
<p><strong>思想</strong>：一次排序可以确定两个值，正向扫描找到最大值交换到最后，反向扫描找到最小值交换到最前面</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-1.png" alt></p>
<blockquote>
<p>一次<mark>循环正反两次</mark>冒泡排序，同时可以用冒泡排序上优化一、二：</p>
<ul>
<li>用一个标记变量记录是否发生了排序</li>
<li>设一个边界变量记录每次冒泡排序的边界，在正向排序找到一个关键值之后需要更新边界，反向排序的时候可以少找一位</li>
</ul>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> bottom=<span class="number">1</span>,top=n;</span><br><span class="line"><span class="keyword">bool</span> flag=<span class="literal">true</span>;</span><br><span class="line"><span class="keyword">int</span> bound=<span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span>(flag)&#123;</span><br><span class="line">	flag=<span class="literal">false</span>;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=bottom;i&lt;top;i++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[i]&gt;arr[i+<span class="number">1</span>])&#123;</span><br><span class="line">			swap(i,i+<span class="number">1</span>);</span><br><span class="line">			flag=<span class="literal">true</span>;</span><br><span class="line">			bound=i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	top=bound;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=top;i&gt;bottom;i--)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[i]&lt;arr[i<span class="number">-1</span>])&#123;</span><br><span class="line">			swap(i,i<span class="number">-1</span>);</span><br><span class="line">			flag=<span class="literal">true</span>;</span><br><span class="line">			bound=i;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	bottom=bound;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="快速排序"><a class="markdownIt-Anchor" href="#快速排序"></a> 快速排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(nlog<sub>2</sub>n)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(nlog<sub>2</sub>n)</td>
</tr>
<tr>
<td>稳定性</td>
<td>不稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：基于<mark>分治</mark>的思想，是冒泡排序的改进型。首先在数组中选择一个基准点（该基准点的选取可能影响快速排序的效率，后面讲解选取的方法），然后设两个指针（<code>low</code>指向起始位置，<code>high</code>指向末尾）分别从数组的两端扫描数组，首先从后半部分扫描，如果发现有元素比该基准点的值小，就交换<code>low</code>和<code>high</code>位置的值，然后从前半部分扫描，发现有元素大于基准点的值，就交换<code>low</code>和<code>high</code>位置的值，如此往复循环，直到<code>low&gt;=high</code>，然后把基准点的值放到<code>high</code>这个位置。一次排序就完成了。以后采用递归的方式再分别对<code>hign</code>前半部分和后半部分排序，当前半部分和后半部分均有序时该数组就自然有序了。</p>
<p><strong>意义</strong>：它是基于关键字比较的<mark>内部排序算法中速度最快者</mark>，快速排序因此而得名</p>
<blockquote>
<p>快速排序主要消耗在区间的划分操作上，每一次划分（循环）结束也就确定了基准点在最终排好序的数列中的绝对位置，所以划分<code>k-1</code>次即完成了对<code>k</code>个数的排序</p>
</blockquote>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">quicksort</span><span class="params">(<span class="keyword">int</span> low,<span class="keyword">int</span> high)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i,j,key;</span><br><span class="line">    <span class="keyword">if</span>(low&gt;high)<span class="keyword">return</span>;</span><br><span class="line">    i=low;j=high;key=arr[low];</span><br><span class="line">    <span class="keyword">while</span>(i!=j)&#123;</span><br><span class="line">        <span class="keyword">while</span>(j&gt;i&amp;&amp;arr[j]&gt;=key)j--;</span><br><span class="line">        <span class="keyword">while</span>(i&lt;j&amp;&amp;arr[j]&lt;=key)i++;</span><br><span class="line">        <span class="keyword">if</span>(i!=j)&#123;</span><br><span class="line">            <span class="keyword">int</span> middle=arr[i];</span><br><span class="line">            arr[i]=arr[j];</span><br><span class="line">            arr[j]=middle;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    arr[low]=arr[i];</span><br><span class="line">    arr[i]=key;</span><br><span class="line">    quicksort(low,i<span class="number">-1</span>);</span><br><span class="line">    quicksort(i+<span class="number">1</span>,high);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	quicksort(<span class="number">1</span>,n);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="选择排序"><a class="markdownIt-Anchor" href="#选择排序"></a> 选择排序</h1>
<p><strong>基本思想</strong>：首先在未排序序列中找到最大（小）元素，存放到排序序列的起始位置，然后，再从剩余未排序元素中继续寻找最大（小）元素，然后放到已排序序列的末尾。以此类推直到所有元素均排序完毕。选择排序也是一种简单直观的排序方法</p>
<p><strong>同交换排序一样仍默认选择排序初始数组从第1位开始存入，第0位作为哨兵位用于交换操作：</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;<span class="comment">//这里定义swap不是STL函数而是手写模拟过程</span></span><br><span class="line">    arr[<span class="number">0</span>]=arr[x];</span><br><span class="line">    arr[x]=arr[y];</span><br><span class="line">    arr[y]=arr[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="简单选择排序"><a class="markdownIt-Anchor" href="#简单选择排序"></a> 简单选择排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>不稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：每次从无序数列中选出最大（小）元素，然后放入有序数列的最末（前）端，该操作执行<code>n-1</code>次</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;=n;i++)&#123;</span><br><span class="line">	<span class="keyword">int</span> d=i;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> j=i+<span class="number">1</span>;j&lt;=n;j++)&#123;</span><br><span class="line">		<span class="keyword">if</span>(arr[j]&lt;arr[d])</span><br><span class="line">		d=j;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span>(d!=i) swap(d,i)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>有一个小的优化：可以每次找出一个最大值和一个最小值，这样循环可以减少一半</p>
</blockquote>
<h2 id="堆排序"><a class="markdownIt-Anchor" href="#堆排序"></a> 堆排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(nlog<sub>2</sub>n)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>不稳定</td>
</tr>
</tbody>
</table>
<p><strong>引入</strong>：简单选择排序进行交换的次数很少，总共才<code>n-1</code>次，但是每次寻找最值的时候都要逐个位置进行比较导致有许多重复比较和无用比较。对于数量多的序列我们可以通过<mark>二叉树堆式建树</mark>可以有效减少比较次数（能利用完全二叉树特性的排序一般性能都不会太差）</p>
<blockquote>
<p><strong>堆的定义</strong>：堆是具有以下性质的完全二叉树——每个结点的值都大于或等于其左右孩子结点的值，称为大顶堆；或者每个结点的值都小于或等于其左右孩子结点的值，称为小顶堆。如下图：</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-2.png" alt></p>
<p>同时，我们对堆中的结点按层进行编号，将这种逻辑结构映射到数组中就是下面这个样子：</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-3.png" alt></p>
<p>该数组从逻辑上讲就是一个堆结构，我们用简单的公式来描述一下堆的定义就是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">&gt;<span class="comment">//若是从0开始存储</span></span><br><span class="line">&gt;大顶堆:arr[i]&gt;=arr[<span class="number">2</span>i+<span class="number">1</span>]&amp;&amp;arr[i&gt;=arr[<span class="number">2</span>i+<span class="number">2</span>]</span><br><span class="line">&gt;小顶堆:arr[i]&lt;=arr[<span class="number">2</span>i+<span class="number">1</span>]&amp;&amp;arr[i]&lt;=arr[<span class="number">2</span>i+<span class="number">2</span>]</span><br><span class="line">&gt;<span class="comment">//若是从1开始存储</span></span><br><span class="line">&gt;大顶堆:arr[i]&gt;=arr[<span class="number">2</span>i]&amp;&amp;arr[i]&gt;=arr[<span class="number">2</span>i+<span class="number">1</span>]</span><br><span class="line">&gt;小顶堆:arr[i]&lt;=arr[<span class="number">2</span>i]&amp;&amp;arr[i]&lt;=arr[<span class="number">2</span>i+<span class="number">1</span>]</span><br></pre></td></tr></table></figure>
<p>**规律：**堆的子节点又是一个小堆的堆顶，由n个元素组成的堆由<code>n/2</code>个中间节点和<code>n-(n/2)</code>个叶节点</p>
</blockquote>
<p><strong>思想</strong>：将待排序序列构造成一个大顶堆，此时整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余<code>n-1</code>个元素重新构造成一个堆，这样会得到<code>n</code>个元素的次小值。如此反复执行<code>n-1</code>次，便能得到一个有序序列了</p>
<blockquote>
<p>第i趟排序将序列前<code>n-i+1</code>个元素组成的子序列转换为一个堆，然后将堆得第一个元素与最后一个元素交换位置，排序需要进行一次堆的初始化和<code>n-1</code>次堆调整、堆顶与堆底交换，每次堆交换确定一个值的绝对位置</p>
</blockquote>
<p><strong>步骤</strong>：1.将原始序列转换为一个堆 2.将堆的第一个元素与堆的最后一个元素进行交换（相当于“去掉“一个最大（小）值）3.将“去掉”最大（小）值元素后剩余的元素组成的子序列重新转换一个新的堆</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">HeapAdjust</span><span class="params">(<span class="keyword">int</span> s,<span class="keyword">int</span> m)</span></span>&#123;<span class="comment">//s是堆的第一个元素，m是堆的最后一个元素</span></span><br><span class="line">    arr[<span class="number">0</span>]=arr[s];<span class="comment">//将堆的第一个值赋给哨兵位置</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">2</span>*s;j&lt;=m;j*=<span class="number">2</span>)&#123;</span><br><span class="line">        <span class="keyword">if</span>(j&lt;m&amp;&amp;arr[j]&lt;arr[j+<span class="number">1</span>])j++;<span class="comment">//找出孩子中最大的 </span></span><br><span class="line">        <span class="keyword">if</span>(arr[<span class="number">0</span>]&gt;=arr[j])<span class="keyword">break</span>;<span class="comment">//符合大顶推退出调整</span></span><br><span class="line">        arr[s]=arr[j];<span class="comment">//把较大的字节点往上移动替换它的父节点 </span></span><br><span class="line">        s=j;<span class="comment">//更新当前要移动的点要放的位置</span></span><br><span class="line">    &#125;</span><br><span class="line">    arr[s]=arr[<span class="number">0</span>];<span class="comment">//交换 </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=n/<span class="number">2</span>;i&gt;<span class="number">0</span>;i--)<span class="comment">//建堆从第n/2个位置依次往上调整(建堆本身就是堆调整)</span></span><br><span class="line">    HeapAdjust(i,n);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=n;i&gt;<span class="number">1</span>;i--)&#123;<span class="comment">//交换堆顶和堆底，执行n-1次</span></span><br><span class="line">        swap(<span class="number">1</span>,i);</span><br><span class="line">        HeapAdjust(<span class="number">1</span>,i<span class="number">-1</span>);<span class="comment">//堆调整从第一个位置调整 </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>建堆的时候因为是无序的，所以凡是有子节点的（即构成堆的）都需要调整，所以从<code>n/2</code>位置开始向上注意每个节点调整。其中构建初始堆经推导复杂度为O(n)，在交换并重建堆的过程中，需交换<code>n-1次</code>，而重建堆的过程中，根据完全二叉树的性质，<code>[log2(n-1),log2(n-2)...1]</code>逐步递减，近似为nlogn。所以堆排序时间复杂度一般认为就是O(nlogn)级。</p>
</blockquote>
<h1 id="stl排序"><a class="markdownIt-Anchor" href="#stl排序"></a> STL排序</h1>
<h2 id="目录"><a class="markdownIt-Anchor" href="#目录"></a> 目录</h2>
<p>所有函数均支持传入比较函数，并且执行给定区间是<mark>左开右闭</mark> <code>[begin,end)</code></p>
<table>
<thead>
<tr>
<th>排序函数</th>
<th>功能描述</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>sort</code></td>
<td>对给定区间所有元素进行不稳定排序（本质：快速排序）</td>
</tr>
<tr>
<td><code>stable_sort</code></td>
<td>对给定区间所有元素进行稳定排序（本质：归并排序）</td>
</tr>
<tr>
<td><code>partial_sort</code></td>
<td>对给定区间所有元素的部分区间元素排序</td>
</tr>
<tr>
<td><code>partial_sort_copy</code></td>
<td>对给定区间复制并进行<code>partial_sort</code></td>
</tr>
<tr>
<td><code>nth_element</code></td>
<td>找出给定区间的指定位置对应的元素</td>
</tr>
<tr>
<td><code>is_sorted</code></td>
<td>判断给定区间是否已经排好顺序</td>
</tr>
<tr>
<td><code>partition</code></td>
<td>对给定区间所有元素按指定条件分类（符合条件的元素放前面）</td>
</tr>
<tr>
<td><code>heap</code>堆排序</td>
<td>堆排序的STL实现，是一系列函数</td>
</tr>
<tr>
<td><code>stable_partition</code></td>
<td>相对稳定的<code>partition</code></td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>比较函数</th>
<th>功能描述</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>equal_to</code></td>
<td>相等</td>
</tr>
<tr>
<td><code>not_equal_to</code></td>
<td>不相等</td>
</tr>
<tr>
<td><code>less</code></td>
<td>小于（升序、大顶堆）</td>
</tr>
<tr>
<td><code>greater</code></td>
<td>大于（降序、小顶堆）</td>
</tr>
<tr>
<td><code>less_qual</code></td>
<td>小于等于</td>
</tr>
<tr>
<td><code>greater_equal</code></td>
<td>大于等于</td>
</tr>
<tr>
<td><code>cmp</code>（自己取名字）</td>
<td>自定义重载比较函数</td>
</tr>
</tbody>
</table>
<p><strong>注意：传入比较函数均是重载方式：</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vector&lt;int&gt;vect;</span><br><span class="line">sort(vect.begin(),vect.end());</span><br><span class="line">sort(vect.begin(),vect.end(),less&lt;int&gt;());</span><br></pre></td></tr></table></figure>
<h2 id="详讲"><a class="markdownIt-Anchor" href="#详讲"></a> 详讲</h2>
<h3 id="sort和stable_sort"><a class="markdownIt-Anchor" href="#sort和stable_sort"></a> <code>sort()</code>和<code>stable_sort()</code></h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x&gt;y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line">    <span class="comment">//对数组排序</span></span><br><span class="line">	<span class="comment">//对[a,a+5)区间的所有元素进行排序 </span></span><br><span class="line">	sort(a,a+<span class="number">5</span>);<span class="comment">//默认升序排列 </span></span><br><span class="line">	sort(a,a+<span class="number">5</span>,greater&lt;<span class="keyword">int</span>&gt;());<span class="comment">//传入降序比较函数 </span></span><br><span class="line">	sort(a,a+<span class="number">5</span>,cmp);<span class="comment">//自定义比较函数 </span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//对容器排序</span></span><br><span class="line">    <span class="comment">//先是赋值</span></span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; v;</span><br><span class="line">    v.assign(a,a+<span class="number">5</span>);</span><br><span class="line">    <span class="comment">//vector&lt;int&gt; a(a,a+5);//相同操作</span></span><br><span class="line">    soort(v.begin(),v.end());</span><br><span class="line">    <span class="comment">//迭代器遍历输出</span></span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it;</span><br><span class="line">    <span class="keyword">for</span>(it=v.begin();it!=v.end();it++)</span><br><span class="line">    <span class="built_in">cout</span>&lt;&lt;*it&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>模板使用的是<code>sort()</code>，<code>stable_sort()</code>同理</p>
</blockquote>
<table>
<thead>
<tr>
<th></th>
<th><code>sort()</code></th>
<th><code>stable_sort()</code></th>
</tr>
</thead>
<tbody>
<tr>
<td>平均时间复杂度</td>
<td>O(nlogn)</td>
<td>O(nlongn)</td>
</tr>
<tr>
<td>本质</td>
<td>快速排序</td>
<td>归并排序</td>
</tr>
</tbody>
</table>
<h3 id="partial_sort和nth_element"><a class="markdownIt-Anchor" href="#partial_sort和nth_element"></a> <code>partial_sort()</code>和<code>nth_element()</code></h3>
<h4 id="partial_sort样例"><a class="markdownIt-Anchor" href="#partial_sort样例"></a> <code>partial_sort()</code>样例</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x&gt;y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;vec(a,a+<span class="number">5</span>);<span class="comment">//赋值 </span></span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator b=vec.begin();</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator e=vec.end();</span><br><span class="line">	</span><br><span class="line">	partial_sort(b,b+<span class="number">3</span>,e);<span class="comment">//部分排序,默认升序排序 </span></span><br><span class="line">	partial_sort(b,b+<span class="number">3</span>,e,greater&lt;<span class="keyword">int</span>&gt;());<span class="comment">//传入降序比较函数</span></span><br><span class="line">	partial_sort(b,b+<span class="number">3</span>,e,cmp); </span><br><span class="line">	<span class="comment">//对[b,e)的子区间[b,b+3)的所有元素进行排序 </span></span><br><span class="line">	</span><br><span class="line">    <span class="keyword">while</span>(b!=e)<span class="comment">//输出</span></span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;*(b++)&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="nth_element样例"><a class="markdownIt-Anchor" href="#nth_element样例"></a> <code>nth_element()</code>样例</h4>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">cmp</span><span class="params">(<span class="keyword">int</span> x,<span class="keyword">int</span> y)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> x&gt;y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;vec(a,a+<span class="number">5</span>);<span class="comment">//赋值 </span></span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator b=vec.begin();</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator e=vec.end();</span><br><span class="line">	</span><br><span class="line">	nth_element(b,b+<span class="number">3</span>,e);<span class="comment">//找出第四小的并放在第四个位置，默认是升序</span></span><br><span class="line"></span><br><span class="line">	<span class="comment">/*(a+l,a+k,a+r)它会使a这个数组中的区间[l,r)中的第k大的元素处于第k个位置上(相对位置）第n个元素之前的元素都小于它，但不必是有序的。同样第n个元素后的元素都大于它，但也不必是有序的。如果第二个参数和第三个参数相同（元素段的末尾），这时这个算法是无效的*/</span></span><br><span class="line">	</span><br><span class="line">	nth_element(b,b+<span class="number">3</span>,e,greater&lt;<span class="keyword">int</span>&gt;());<span class="comment">//找出第四大的数并放在第四个位置</span></span><br><span class="line">	nth_element(b,b+<span class="number">3</span>,e,cmp);</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;<span class="string">"输出第四小的数："</span>&lt;&lt;vec[<span class="number">3</span>]&lt;&lt;<span class="built_in">endl</span>; </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>注意：函数中间的那个指定的是<mark>相对位置</mark>，因为数组从0开始编号，所以b+3是第四个位置，放的是第四大的数</strong></p>
<h3 id="partion和stable_partition"><a class="markdownIt-Anchor" href="#partion和stable_partition"></a> <code>partion()</code>和<code>stable_partition()</code></h3>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsOdd</span><span class="params">(<span class="keyword">int</span> i)</span></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> (i%<span class="number">2</span>)==<span class="number">1</span>;<span class="comment">//奇数返还true，偶数返还false </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line">	<span class="function"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; <span class="title">vec</span><span class="params">(a,a+<span class="number">5</span>)</span></span>;</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator bound=partition(vec.begin(),vec.end(),IsOdd);</span><br><span class="line">	<span class="comment">//bound是第一个不满足条件的数，bound前面的数都是奇数，bound本身以及后面的数是偶数 </span></span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;<span class="string">"Odd elements:"</span>;</span><br><span class="line">	<span class="comment">//输出奇数</span></span><br><span class="line">	<span class="keyword">for</span>(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it=vec.begin();it!=bound;it++)</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;*it&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">	<span class="comment">//输出偶数 </span></span><br><span class="line">	<span class="keyword">for</span>(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it=bound;it&lt;vec.end();it++)</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;*it&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//另一个例子，找出数组中比平均值小的数并且按照降序排列</span></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> a[]=&#123;<span class="number">1</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>&#125;;</span><br><span class="line">	<span class="function"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; <span class="title">vec</span><span class="params">(a,a+<span class="number">5</span>)</span></span>;</span><br><span class="line">	<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it,bound;</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">int</span> average=accumulate(vec.begin(),vec.end(),<span class="number">0</span>)/vec.size();<span class="comment">//加和函数</span></span><br><span class="line">	bound=partition(vec.begin(),vec.end(),bind2nd(less&lt;<span class="keyword">int</span>&gt;(),average));</span><br><span class="line"></span><br><span class="line">	<span class="comment">//升序输出比平均值小的数 </span></span><br><span class="line">	<span class="keyword">for</span>(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it=vec.begin();it!=bound;it++)</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;*it&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">	<span class="comment">//输出其他的值，没有顺序 </span></span><br><span class="line">	<span class="keyword">for</span>(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;::iterator it=bound;it&lt;vec.end();it++)</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;*it&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>模板使用的是<code>partion()</code>，<code>stable_partition()</code>同理</p>
</blockquote>
<blockquote>
<p><strong>关于bind1nd()和bind2nd()的讲解</strong><br>
首先，他们都在头文件<code>&lt;functional&gt;</code>中定义。<br>
其次，bind就是绑定的意思，而1st就代表<code>first</code>，2nd就代表<code>second</code>。<br>
再次，他们的声明是一样的，都是<code>(const Operation&amp; op, const T&amp; x)</code>。</p>
<p>简单的说，<code>bind1st(const Operation&amp; op, const T&amp; x)</code>就是这么一个操作：<code>x op value</code>，而<code>bind2nd(const Operation&amp; op, const T&amp; x)</code>就是这么一个操作：<code>value op x</code>，其中<code>value</code>是被应用<code>bind</code>的对象。这两个配接器都用于将一个二元算子转换成一个一元算子。下面来看一段代码吧！</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">&gt;<span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line">&gt;<span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line">&gt;<span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function">&gt;</span>&#123;</span><br><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; coll;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">10</span>; ++i)&#123;</span><br><span class="line">	coll.push_back(i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//查找元素值大于10的元素的个数</span></span><br><span class="line"><span class="comment">//也就是使得10 &lt; elem成立的元素个数</span></span><br><span class="line"><span class="keyword">int</span> res = count_if(coll.begin(), coll.end(), bind1st(less&lt;<span class="keyword">int</span>&gt;(), <span class="number">10</span>));</span><br><span class="line">	<span class="comment">/*less是二元函数对象，bind1st(less&lt;int&gt;(), 10)就是将10赋值给less模板中的left；</span></span><br><span class="line"><span class="comment">bind2nd(less&lt;int&gt;(), 10))中的10赋值给了right*/</span></span><br><span class="line">	<span class="built_in">cout</span> &lt;&lt; res &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"><span class="comment">//查找元素值小于10的元素的个数(也可以看做第一个不满足条件的数的位置)</span></span><br><span class="line">res = count_if(coll.begin(), coll.end(), bind2nd(less&lt;<span class="keyword">int</span>&gt;(), <span class="number">10</span>));</span><br><span class="line"><span class="built_in">cout</span> &lt;&lt; res &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&gt;&#125;</span><br><span class="line"></span><br><span class="line">&gt;<span class="comment">/*输出结果:</span></span><br><span class="line"><span class="comment">&gt;0</span></span><br><span class="line"><span class="comment">&gt;9*/</span></span><br></pre></td></tr></table></figure>
</blockquote>
<h3 id="heap排序"><a class="markdownIt-Anchor" href="#heap排序"></a> <code>heap</code>排序</h3>
<p><code>make_heap()</code>：根据指定的迭代器区间以及一个可选的比较函数来创建一个<code>heap</code>（默认建立大顶堆）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; vec&#123;<span class="number">6</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line">make_heap(vec.begin(),vec.end());<span class="comment">//6 5 4 1 3 2</span></span><br><span class="line">make_heap(vec.beign(),vec.end(),greater&lt;<span class="keyword">int</span>&gt;());<span class="comment">//1 3 2 5 6 4 2</span></span><br></pre></td></tr></table></figure>
<p><code>push_heap()</code>：把指定区间的最后一个元素插入到<code>heap</code>中</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">vec.push_back(<span class="number">200</span>);<span class="comment">//5 6 1 3 2 4 200</span></span><br><span class="line">push_heap(vec.begin(),vec.end());<span class="comment">//200 5 6 1 3 2 4</span></span><br></pre></td></tr></table></figure>
<p><strong>注意：使用<code>push_heap(f,l)</code>的话一定要确保<code>[f,l-1)</code>已经是一个堆（第l个元素是要排入堆中的元素），<code>push_heap(f,l)</code>仅仅会把第<code>(l-1)</code>的数插入到<code>[f,l-1)</code>这个区间形成的堆中</strong></p>
<p><code>pop_heap()</code>：弹出<code>heap</code>顶元素，将其放置于区间末尾</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;vec&#123;<span class="number">6</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line">make_heap(vec.begin(),vec.end());<span class="comment">//6 5 4 1 3 2</span></span><br><span class="line">pop_heap(vec.begin(),vec.end());<span class="comment">//5 4 1 3 2 6</span></span><br><span class="line">vec.pop_back();<span class="comment">//6 5 4 3 2 1</span></span><br></pre></td></tr></table></figure>
<p><code>sort_heap()</code>：堆排序算法，通常通过<mark>反复调用</mark><code>pop_heap()</code>交换堆顶和堆底来实现</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; vec&#123;<span class="number">6</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">5</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;</span><br><span class="line">make_heap(vec.begin(),vec.end());<span class="comment">//6 5 4 1 3 2</span></span><br><span class="line">sort_heap(vec.beign(),vec.end());<span class="comment">//6 5 4 3 2 1</span></span><br></pre></td></tr></table></figure>
<p><code>heap()</code>使用示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">signed main()</span><br><span class="line">&#123;</span><br><span class="line">	int a[10]&#x3D;&#123;1,5,2,6,3,7,8,4,9,0&#125;;</span><br><span class="line">	vector&lt;int&gt; vec(a,a+10);</span><br><span class="line">	</span><br><span class="line">	make_heap(vec.begin(),vec.end(),less&lt;int&gt;());&#x2F;&#x2F;建立小顶堆 </span><br><span class="line">	vector&lt;int&gt;::iterator it;</span><br><span class="line">	for(it&#x3D;vec.begin;it!&#x3D;vec.end();it++)</span><br><span class="line">	cout&lt;&lt;*it&lt;&lt;&quot; &quot;;&#x2F;&#x2F;输出大顶堆 </span><br><span class="line">	</span><br><span class="line">	vec.push_back(200);&#x2F;&#x2F;加入一个200 </span><br><span class="line">	push_heap(vec.begin(),vec.end());&#x2F;&#x2F;调整大顶堆 </span><br><span class="line">    &#x2F;&#x2F;也可以用sort_heap(vec.begin(),vec.end())</span><br><span class="line">	</span><br><span class="line">	for(it&#x3D;vec.begin;it!&#x3D;vec.end();it++)</span><br><span class="line">	cout&lt;&lt;*it&lt;&lt;&quot; &quot;;</span><br><span class="line">	return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="其他排序"><a class="markdownIt-Anchor" href="#其他排序"></a> 其他排序</h1>
<h2 id="归并排序"><a class="markdownIt-Anchor" href="#归并排序"></a> 归并排序</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(nlog<sub>2</sub>n)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(n)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：该算法先采用分治法将复杂的多数量排序问题分为若干个简单的少数量排序问题递归求解，然后在将解决的问题逐渐合并最终得到整体排序问题答案</p>
<blockquote>
<p>分：将问题分成一些小的问题然后递归求解</p>
<p>治：将分的阶段得到的各答案&quot;修补&quot;在一起</p>
</blockquote>
<p><strong>图解</strong>：先分而治之然后在将其合并</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-4.png" alt></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-5.png" alt></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> a[<span class="number">10</span>]=&#123;<span class="number">9</span>,<span class="number">8</span>,<span class="number">7</span>,<span class="number">6</span>,<span class="number">5</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">1</span>,<span class="number">0</span>&#125;,b[<span class="number">10</span>];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mergesort</span><span class="params">(<span class="keyword">int</span> l,<span class="keyword">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l==r)<span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">int</span> m=(l+r)/<span class="number">2</span>;</span><br><span class="line">    mergesort(l,m);</span><br><span class="line">    mergesort(m+<span class="number">1</span>,r);</span><br><span class="line">    <span class="keyword">int</span> lp=l,rp=m+<span class="number">1</span>,s=<span class="number">0</span>;</span><br><span class="line">    <span class="comment">//lp:[l,p]区间取到了lp，rp：[m+1,r]取到了rp</span></span><br><span class="line">    <span class="comment">//s:选出的数的个数</span></span><br><span class="line">    <span class="keyword">while</span>(lp&lt;=m&amp;&amp;rp&lt;=r)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(a[lp]&lt;a[rp])</span><br><span class="line">        &#123;</span><br><span class="line">            b[s++]=a[lp++];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(a[lp]&gt;a[rp])</span><br><span class="line">        &#123;</span><br><span class="line">            b[s++]=a[rp++];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="keyword">while</span>(lp&lt;=m)</span><br><span class="line">    b[s++]=a[lp++];</span><br><span class="line">    <span class="keyword">while</span>(rp&lt;=r)</span><br><span class="line">    b[s++]=a[rp++];</span><br><span class="line">    <span class="comment">//最后把b数组中排好序的数列赋值会a数组中</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=l;i&lt;=r;i++)</span><br><span class="line">    a[i]=b[i-l]; <span class="comment">//注意要以排序的左边界l作为临界，因为可能数组只是部分排序 </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">signed</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	mergesort(<span class="number">1</span>,<span class="number">9</span>);</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">10</span>;i++)</span><br><span class="line">	<span class="built_in">cout</span>&lt;&lt;a[i]&lt;&lt;<span class="string">" "</span>;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="gnome排序地精排序"><a class="markdownIt-Anchor" href="#gnome排序地精排序"></a> <code>GNOME</code>排序（地精排序）</h2>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n<sup>2</sup>)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(1)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：通过一层循环进行遍历，相邻的两个元素比较，如果两个元素逆序则交换。与冒泡排序不同的是他如果遇到一个交换操作的时候变为向前冒泡，直至不发生交换操作位置。所以地精排序可以说是冒泡排序和直接插入排序的组合</p>
<blockquote>
<p>循环的指针<code>i</code>从0开始，进入循环。当<code>i</code>是0的时候直接<code>i++</code>，当i不为0的时候比比较下标为<code>i</code>和<code>i-1</code>的数值，如果正序就<code>i++</code>,如果逆序就交换并且<code>i--</code>。一直循环至<code>i</code>到最后一个元素的位置。</p>
</blockquote>
<p><strong>图解</strong></p>
<table>
<thead>
<tr>
<th style="text-align:left">当前数组</th>
<th style="text-align:left">操作</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">[<strong>5</strong>, 3, 2, 4]</td>
<td style="text-align:left"><code>pos==0</code>直接<code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[5, <strong>3</strong>, 2, 4]</td>
<td style="text-align:left"><code>a[pos]&lt;a[pos-1]</code>，交换并且<code>pos&gt;0</code>，<code>pos</code>自减</td>
</tr>
<tr>
<td style="text-align:left">[3, <strong>5</strong>, 2, 4]</td>
<td style="text-align:left"><code>a[pos]&gt;=a[pos-1]</code>， <code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[3, 5, <strong>2</strong>, 4]</td>
<td style="text-align:left"><code>a[pos]&lt;a[pos-1]</code>，交换并且<code>pos&gt;0</code>, <code>pos</code>自减</td>
</tr>
<tr>
<td style="text-align:left">[3, <strong>2</strong>, 5, 4]</td>
<td style="text-align:left"><code>a[pos]&lt;a[pos-1]</code>，交换并且<code>pos&gt;0</code>,<code>pos</code>自减</td>
</tr>
<tr>
<td style="text-align:left">[<strong>2</strong>, 3, 5, 4]</td>
<td style="text-align:left"><code>pos==0</code>直接<code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[2, <strong>3</strong>, 5, 4]</td>
<td style="text-align:left"><code>a[pos]&gt;=a[pos-1]</code>，<code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[2, 3, <strong>5</strong>, 4]</td>
<td style="text-align:left"><code>a[pos]&gt;=a[pos-1]</code>,  <code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[2, 3, 5, <strong>4</strong>]</td>
<td style="text-align:left"><code>a[pos]&lt;a[pos-1]</code>，交换并且<code>pos&gt;0</code>, <code>pos</code>自减</td>
</tr>
<tr>
<td style="text-align:left">[2, 3, <strong>4</strong>, 5]</td>
<td style="text-align:left">a[pos] &gt;=a[pos-1]，<code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[2, 3, 4, <strong>5</strong>]</td>
<td style="text-align:left">a[pos]&gt;=a[pos-1]，<code>pos</code>自增</td>
</tr>
<tr>
<td style="text-align:left">[2, 3, 4, 5]</td>
<td style="text-align:left"><code>pos==length(a)</code>，排序完成</td>
</tr>
</tbody>
</table>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//地精算法改进版</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">GnomeSort</span><span class="params">(<span class="keyword">int</span> a[],<span class="keyword">int</span> len)</span></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> i=<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">while</span>(i&lt;=len)&#123;</span><br><span class="line">		<span class="keyword">if</span>(i==<span class="number">0</span>||a[i]&gt;=a[i<span class="number">-1</span>])i++;</span><br><span class="line">		<span class="keyword">else</span>&#123;</span><br><span class="line">			swap(a[i],a[i<span class="number">-1</span>]);</span><br><span class="line">			i--;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>地精排序最显著的特点是代码只有一层循环，原版算法代码极短，但效率不高，改进后相当于插入排序的变种，不过条件判断多了，代码比标准的插入排序长</p>
</blockquote>
<h2 id="非比较排序"><a class="markdownIt-Anchor" href="#非比较排序"></a> 非比较排序</h2>
<p><strong>基础</strong>：非比较排序是一种基于桶排序思想的<mark>高效线性排序</mark>，适用对<mark>数据范围小但数量多</mark>的序列排序</p>
<p><strong>意义</strong>：在一定情况下，非比较排序效率要比比较排序更高，是一种牺牲空间换取时间的做法</p>
<h3 id="桶排序思想"><a class="markdownIt-Anchor" href="#桶排序思想"></a> 桶排序思想</h3>
<p>桶排序是一种排序的思想，其实现包括计数排序和基数排序两种，冒泡排序、选择排序、插入排序、归并排序、快速排序和堆排序都是基于比较的排序，桶排序提出了一种新的思路，即基于<mark>数据状态</mark>的排序</p>
<p><strong>注意</strong>：桶是一种容器，这个容器可以用多种数据结构实现，包括数组、队列或者栈</p>
<p><strong>算法实现</strong>：计数排序和基数排序</p>
<p><strong>步骤</strong>：1.得到无序数组的取值范围</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-6.webp" alt></p>
<p>2.根据取值范围创建对应数量的桶</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-7.webp" alt></p>
<p>3.遍历数组，把每个元素放到对应的桶中</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-8.webp" alt></p>
<p>(4）按照顺序遍历桶中的每个元素，依次放到数组中，即可完成数组的排序</p>
<h3 id="计数排序msd"><a class="markdownIt-Anchor" href="#计数排序msd"></a> 计数排序（<code>MSD</code>）</h3>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n+k)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(n+k)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p><strong>思想</strong>：将出现的数放入指定的桶中，然后按照桶的顺序依次输出里面的数即排好了顺序</p>
<p><strong>步骤</strong>：1.找出无序数组的最大值，创建一个长度为数据中最大值+1的空数组</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-9.webp" alt></p>
<p>2.遍历原数组，统计每个元素出现的次数</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-10.webp" alt></p>
<p>3.遍历桶，即用于元素个数统计的数组，得到有序的数组</p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-11.webp" alt></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">CountSort</span><span class="params">(<span class="keyword">int</span> arr[],<span class="keyword">int</span> n)</span></span>&#123;<span class="comment">//范围为0-20 </span></span><br><span class="line">	<span class="keyword">int</span> bucket[<span class="number">21</span>]=&#123;<span class="number">0</span>&#125;;</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)</span><br><span class="line">	bucket[arr[i]]++;</span><br><span class="line">	<span class="built_in">memset</span>(arr,<span class="number">0</span>,<span class="keyword">sizeof</span> arr);<span class="comment">//清空装有序列表 </span></span><br><span class="line">	<span class="keyword">int</span> cnt=<span class="number">1</span>;	</span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">21</span>;i++)&#123;</span><br><span class="line">		<span class="keyword">while</span>(bucket[i])&#123;<span class="comment">//若是不为空就存入arr[] </span></span><br><span class="line">			arr[cnt++]=i;</span><br><span class="line">			bucket[i]--;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="基数排序lsd"><a class="markdownIt-Anchor" href="#基数排序lsd"></a> 基数排序（<code>LSD</code>）</h3>
<table>
<thead>
<tr>
<th>属性</th>
<th>参数</th>
</tr>
</thead>
<tbody>
<tr>
<td>时间复杂度</td>
<td>O(n*k)</td>
</tr>
<tr>
<td>空间复杂度</td>
<td>O(n+k)</td>
</tr>
<tr>
<td>稳定性</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<p>计数排序是每个桶只存储单一键值，而基数排序是根据键值的每位数字来分配桶</p>
<p><strong>思想</strong>：将整数按位数切割成不同的数字，然后按每个位数分别比较。由于整数也可以表达字符串（比如名字或日期）和特定格式的浮点数，所以基数排序也不是只能使用于整数</p>
<ul>
<li>[ ] 代码还不会，待整理 😓</li>
</ul>
<h1 id="排序总结"><a class="markdownIt-Anchor" href="#排序总结"></a> 排序总结</h1>
<p><strong>本博客总结的排序都是内部排序（只运用内存进行排序）</strong></p>
<p><img src= "/img/loading.gif" data-src="https://gitee.com/pengpenglang/blogimg/raw/master/img/sort-12.png" alt></p>
<table>
<thead>
<tr>
<th>排序方法</th>
<th>时间复杂度（最好）</th>
<th>时间复杂度（最坏）</th>
<th>时间复杂度（平均）</th>
<th>空间复杂读</th>
<th>稳定性</th>
</tr>
</thead>
<tbody>
<tr>
<td>插入排序</td>
<td>O(n)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(1)</td>
<td>稳定</td>
</tr>
<tr>
<td>希尔排序</td>
<td>O(n)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>1.3</sup>)</td>
<td>O(1)</td>
<td>不稳定</td>
</tr>
<tr>
<td>选择排序</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(1)</td>
<td>不稳定</td>
</tr>
<tr>
<td>堆排序</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(1)</td>
<td>不稳定</td>
</tr>
<tr>
<td>冒泡排序</td>
<td>O(n)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(1)</td>
<td>稳定</td>
</tr>
<tr>
<td>快速排序</td>
<td>O(nlong<sub>2</sub>n)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>不稳定</td>
</tr>
<tr>
<td>归并排序</td>
<td>O(n)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(nlog<sub>2</sub>n)</td>
<td>O(n)</td>
<td>稳定</td>
</tr>
<tr>
<td>地精排序</td>
<td>O(n2^)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(n<sup>2</sup>)</td>
<td>O(1)</td>
<td>稳定</td>
</tr>
<tr>
<td>计数排序</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>O(n+k)</td>
<td>稳定</td>
</tr>
<tr>
<td>基数排序</td>
<td>O(n*k)</td>
<td>O(n*k)</td>
<td>O(n*k)</td>
<td>O(n+k)</td>
<td>稳定</td>
</tr>
</tbody>
</table>
<h1 id="神级排序"><a class="markdownIt-Anchor" href="#神级排序"></a> 神级排序</h1>
<p><code>Bogo</code>排序，又被称为猴子排序，是一种恶搞排序算法，它将一切交给了上帝，其算法就是坑爹的将元素随机打乱，然后紧紧检查其是否符合排列顺序，若否，则继续进行随机打乱，继续检查结果，直到符合排列顺序。<br>
<code>Bogo</code>排序的最坏时间复杂度为O(∞)，一辈子也不能输出排序结果，平均时间复杂度为O(n·n!)。</p>
<p>然而，有个看似笑话的方法声称可以用O(n)实现<code>Bogo</code>排序，依照量子理论的平行宇宙解释，使用量子随机性随机地重新排列元素，不同的可能性将在不同的宇宙中展开，总有一种可能猴子得到了正确的顺序，量子计算机找到了这个宇宙后，就开始毁灭其他排序不成功的宇宙，剩下一个观察者可以看到的正确顺序的宇宙。</p>
<p>如果想要迈出这个看似荒诞，但令人无比兴奋的&quot;高效算法&quot;的第一步，请先证明&quot;平行宇宙解释&quot;的正确性。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">BOOL <span class="title">BogoSort</span><span class="params">(datatype *<span class="built_in">array</span>, <span class="keyword">int</span> size)</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i, j;</span><br><span class="line">    <span class="keyword">int</span> tag;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">array</span> == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> FALSE;</span><br><span class="line">    &#125;</span><br><span class="line">    srand((<span class="keyword">unsigned</span> <span class="keyword">int</span>)time(<span class="literal">NULL</span>));</span><br><span class="line">    <span class="keyword">while</span>(TRUE) &#123;</span><br><span class="line">        tag = TRUE;</span><br><span class="line">        <span class="comment">//检测</span></span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">1</span>; i &lt; size; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span>(<span class="built_in">array</span>[i] &lt; <span class="built_in">array</span>[i<span class="number">-1</span>]) &#123;</span><br><span class="line">                tag = FALSE;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//如果有序，则排序完成</span></span><br><span class="line">        <span class="keyword">if</span>(tag) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//随机打乱</span></span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; size; i++) &#123;</span><br><span class="line">            j = rand() % size;</span><br><span class="line">            Swap(<span class="built_in">array</span> + i, <span class="built_in">array</span> + j);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> TRUE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>计算机学习</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>算法与数据结构</tag>
      </tags>
  </entry>
</search>
